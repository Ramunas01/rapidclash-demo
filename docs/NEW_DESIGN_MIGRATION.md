# New Design Migration — screen inventory & tracking

Living checklist for migrating every current screen to the new design package (`RapidClash Full Spec.html`, Designer handoff, 2026-09-08). Update this file as each row moves from not-started → in progress → built → pixel-verified. This is the coverage tracker; per-screen pixel fidelity is a separate, later concern (see "Two kinds of verification" below).

## Roles & process for this migration (Owner-agreed 2026-09-09)

The Designer team drives the changes — they ship exact values per screen, so there is little to
arbitrate. For this migration the Advisor/PM split collapses into **scoping+sanity vs execution**:

- **Advisor** (owns `docs/`): owns this tracker, pressure-tests each Designer message against the
  real repo, surfaces traps, tees up Owner decisions, defines each phase's ticket precisely, and
  builds the setup-phase tooling (the fidelity harness). Pushes branches for review; does **not**
  merge.
- **PM**: holds the **review gate and the merge button** for screen-rebuild PRs (the normal
  Advisor→push, Owner→merge rule is delegated to the PM here). Runs the parallel execution phase —
  issues, ≤2 coding agents, review against acceptance criteria — once the repetitive rebuilds
  start. Keeps this tracker's status column honest alongside the board. See
  `docs/COMMS/ADVISOR_TO_PM.md` 2026-09-09#1 for the PM's full brief.
- **Owner**: metric/scope decisions, and the trigger to activate the PM.

**Decisions locked 2026-09-09:**
- Merge gate for screen-rebuild PRs → **PM reviews + merges** (CI green, `/code-review` addressed,
  harness fidelity meets the bar, acceptance criteria met, tracker row updated).
- Deploy cadence → **no deploy until the first user-visible change.** Ships Battle removal +
  prototype landing are invisible; they wait. (Deploy triggers the demo-taker bot-crowd restart.)
- Sort · Popularity metric → **all-time settled-match count per game** (confirmed, no rolling window).
- PRs #456 / #457 / #458 were merged directly by the Advisor under Owner direction on 2026-09-09 —
  a one-time exception; the PM holds merge from here.

## Currency presentation (Owner-approved 2026-09-09) — `CHARTER.md` #4

The registered/investor demo drops the `¢` credit glyph and presents a **`$` symbol plus a
cosmetic multi-currency wallet skin** (the prototype's `curOpen` picker — USD + SOL/BTC/USDT/
ETH/LTC/USDC/XRP, with a fiat/crypto display toggle). **This is presentation only:**

- The ledger, protocol, schema and stored values stay **plain integer demo credits** — exactly
  what `apps/web/src/format.ts`'s own header already says. No FX math, no conversion, no rail,
  no cash-out, no real value. The picker just changes which icon/format wraps the same integer;
  per-currency balances are mock display strings (as in the prototype), not derived from one
  number.
- **Stakes are a fixed 6-rung `$` ladder — $1 / $5 / $10 / $25 / $50 / $100** (the prototype's
  `STAKES` set; its bet selector is literally `/ 6`). This replaces today's free-typed stake
  entry (`StakeEntry.tsx`) and is a real matchmaking improvement, not just a reskin — free-typed
  stakes fragment the pairing pool today. **Size it as its own workstream**, with a shared
  `STAKE_LADDER` constant + matchmaking/validation changes, not folded into a screen rebuild.
- **Guest mode is unchanged** — stays `¢` / play-money-framed (a Board condition, `CHARTER.md`
  line 32). `format.ts` / `<Credits>` keep working for the guest surface; the registered app
  gets the `$` skin alongside, not instead.
- The `~8 "no $" tests` (`RouletteHub.test`, `Wallet.test`'s `not.toMatch(/deposit|crypto/)`,
  `ProfileHub.test` "no $ anywhere", …) flip **organically** as the rebuild PRs touch those
  screens — no separate pass.
- Owners are aware of the framing implications of showing a crypto-styled skin externally
  (Owner confirmed 2026-09-09).

## Before anything else — three things to resolve, not work around

1. **DONE — landed on `main`.** `design/prototype/` (131 files: HTML + 119 `assets/` incl. `assets/export/`'s 14 webp tiles + `image-slot.js`/`support.js` + README + 8 screenshots) merged via **PR #458** (2026-09-09, squash `68fb896`). Plain commit, ~37.7 MB — git-lfs was offered and not taken. Asset triage (the ~61 unreferenced exploration PNGs) is a pending mechanical follow-up.
2. **Done.** The re-extracted copy matches the Designer's counts (39MB, `assets/export/` present, `welcome-banner-v2..v6` present). Asset triage (deleting the ~61 unreferenced exploration files) is still open but no longer blocking — see sizing candidates below.
3. **Resolved — not blocked, and the "one representative per shape" theory in the first draft of this doc was wrong.** Per the Designer: `rps`/`mines`/`dice` are the only 3 with new screens because they're the only 3 **not already finished** — Coinflip, Blackjack, and Chess already match the new design and need no screen work. There's no pattern to generalize from the 3 samples to the rest; see the roster/scope section below for the real three-way split.

## Roster change — RESOLVED: Ships Battle removed (Owner-confirmed 2026-09-09)

Owner confirmed the Designer's call: **Ships Battle is removed from the platform entirely** (hub, route, package, assets, spec, bot-crowd entries — all deleted), bringing the roster to 12 games. **DONE — merged via PR #457** (2026-09-09, squash `f1f9acf`). The full pre-removal implementation is preserved at git tag **`archive/ships-battle`** (`git checkout archive/ships-battle -- packages/games/ships-battle …` to restore if ever reinstated). `CHARTER.md`'s roster table and grouping description, `SCREENS.md`, `REPO_MAP.md` all updated in the same PR. Separate work from the rps/mines/dice rebuild.

## The real roster + scope split (Designer-confirmed, supersedes the "10 games blocked" framing above)

**12-game roster**: coinflip, blackjack, chess, mines, rps, crash, dice, roulette, hilo, keno, baccarat, limbo. Work splits three ways, not "3 done + 10 blocked":

| Group | Games | What actually happens |
|---|---|---|
| New screens | rps, mines, dice | Full rebuild from the new design package — the only 3 with new screens because they're the only 3 not already finished. |
| Already done | coinflip, blackjack, chess | No screen work. **One QA check once rps/mines/dice ship**: if the 3 newly-rebuilt games look visibly different from these 3 (built under the old design), that inconsistency becomes its own follow-up ticket — catch it in the same pass, not after the whole migration ships. |
| Chrome-only | crash, roulette, hilo, keno, baccarat, limbo | No hub rebuild. Tiles keep showing in the lobby; these 6 only need to render correctly under the new shared chrome (header, bottom nav, tokens) once that lands platform-wide — a side effect of the shared-component work, not a separate per-game ticket. |

## Categories are many-to-many, not a per-game field — corrects the original open item

Source: `CAT_GAMES` in the prototype, line 2843 — already assigned by design, not ours to invent:

| Category | Games |
|---|---|
| RAPIDCLASH ORIGINALS | all 12 |
| CARD GAMES | blackjack, hilo, baccarat |
| CHANCE GAMES | coinflip, dice, roulette, keno, limbo, mines, crash, baccarat |
| SKILL GAMES | chess, blackjack, hilo, rps |
| EVENTS | empty by design — has its own `catEmpty` state |

Blackjack sits in 3 categories (Originals, Card, Skill); Baccarat in Card + Chance (+ Originals); Hilo in Card + Skill (+ Originals). **Data model: a game needs a set of category tags, not a single category value.** Worth noting this also restructures `CHARTER.md`'s current grouping taxonomy (today: Originals / Classics / Events, three mutually-exclusive tiers, Chess filed under "Classics") into a many-to-many tag system where "Originals" spans the whole roster — the doc will need updating alongside the data model once this ships, or it'll flatly contradict what's live.

## Two UI elements that are fully designed but functionally undefined — RESOLVED (Owner, 2026-09-09)

Confirmed directly from the prototype's script block; behaviour was ours to define and the Owner has now defined it:

- **Search — RESOLVED.** The input, expand animation, and clear-on-close are fully built, but `searchQuery` is never read by `gridGames` in the prototype. **Decision: adopt the Designer's suggested default — substring match on game *name* across all 12 games, ignoring whichever category tab is selected** (typing "chess" surfaces Chess regardless of the active tab). Case-insensitive, matches as-you-type. Empty query → the normal category-filtered grid.
- **Sort — RESOLVED.** `sortBy` has Popularity (default) / Newest / Alphabetical.
  - **Popularity = all-time settled-match count per `gameId`** (Owner-confirmed 2026-09-09 — no rolling window). All bot-crowd play is bot-vs-human by design, so every settled match is real volume; nothing to exclude. Stable, no scheduled recompute. Source: `match-history.ts` leaderboard/aggregate queries.
  - **Newest = order of introduction onto the platform.** A fixed per-game ordinal, not a live date field. Establish the ordering once from each game package's first-landed commit (git history of `packages/games/<name>/`), encode as a static `introOrder` map/array. New games append.
  - **Alphabetical** — by display name, already well-defined.

## Two kinds of verification — don't conflate them

- **Coverage** (this document): have we addressed every screen/state the redesign touches? Answered by the table below, updated by hand as work proceeds.
- **Fidelity** (the Designer's Playwright proposal): once a specific screen is built, is it pixel-correct against the prototype? A separate, mechanical tool, set up once and reused per screen — sized and scheduled as its own line item below, not bundled into the coverage question.

## Screen inventory

| Current screen(s) | New design `view`/state | Screenshot? | Status | Notes |
|---|---|---|---|---|
| `game-list` / `home` (games hub) | `games` (category rail) | ✅ 01, 02 | Not started | Category rail is now ORIGINALS / CARD GAMES / CHANCE GAMES / SKILL GAMES / EVENTS (was ALL GAMES / ORIGINALS / CLASSICS / EVENTS) — many-to-many per game, see the category table above, not a single field. `MenuOverlay.tsx`'s GAMES group already has placeholder rows named exactly "Card games" / "Chance games" / "Skill games" (from the Menu ticket) — this categorization was already anticipated once and left unwired; this is where it gets resolved for real. |
| *(no current equivalent)* | search-open overlay | ✅ 03 | Not started | Net new. Behavior is ours to define — see "fully designed but functionally undefined" above; UI/animation is done, matching is not. |
| *(no current equivalent)* | sort sheet (`sortOpen`) | ✅ 04 | Not started | Net new. Same situation as search — the sheet UI is done, the actual reordering logic isn't, and "Popularity"/"Newest" need a concrete metric decided first. |
| `rewards` (RewardsHub.tsx) | `rewards` | ✅ 05 (signed-out only) | Not started | RewardsHub was already redesigned + rakeback-fixed this cycle — this is a second pass on recently-shipped work. Signed-in state not captured; reachable by click-through per the Designer. |
| `auth` (AuthModal) | `authOpen` (`loggedIn`, `authMode`, email/password/referral fields) | ✅ 06 | Not started | **Resolved: fully replaces the existing modal**, not scoped to one entry point — confirmed the only auth surface in the export. **New scope worth flagging on its own**: the field list includes a `referral` field, not present in today's `AuthModal` — registration capturing a referral code ties directly into Affiliate's attribution story. Worth confirming with the Designer/Owner whether this needs real backend wiring now or can be session-local/cosmetic for now, matching the mock-data approach the rest of Affiliate already uses — don't assume either way. |
| `menu` overlay (MenuOverlay.tsx) | `menuOpen` | ✅ 07 | Not started | Also a second pass on recently-shipped work (built a few tickets ago). |
| `profile` (ProfileHub.tsx) | `account` | ✅ 09 (signed-in) + 06 (signed-out/login sheet) | Not started | Third redesign pass on this screen this cycle (original build → icon fix → this). |
| `preferences` (PreferencesHub.tsx) | `prefs` | ❌ none | Not started | Click-through only per README. **Light-theme token table exists for this screen in the spec — see the standing light-theme question below before starting.** |
| `affiliate` (AffiliateHub.tsx) | `affiliates` | ❌ none | Not started | Click-through only. This screen has had three consecutive correction tickets this cycle already (hero rebuild in progress, invented-tiers removal) — confirm with Designer whether this new pass supersedes or builds on that in-flight work before starting, so it isn't rebuilt twice. |
| Chat nav item (permanently inert placeholder) | `chatOpen` | ❌ none | **Fully scoped 2026-09-11 — `ADVISOR_TO_PM.md` 2026-09-11#6 — two decisions needed before ticketing** | Full UI/interactions built for real (sending, @mentions, channel switching, half-height mode, VIP colours, seeded backlog); transport stays ephemeral (in-memory, gone on server restart — genuine live cross-player messaging, not client-only) behind a swappable `ChatTransport` interface, same pattern as `EphemeralLedger extends Ledger`. Server-checked kill switch (not a client-bundled env var). The original `chat-local-transport.md` this row cited no longer exists on disk (gitignored scratch from an earlier session); 2026-09-11#6 rebuilds the scoping from the prototype source directly and flags the two things that doc apparently already decided but can't be re-verified: room scope (one global room vs. per-game) and exactly what "server-checked" needs to mean. Deliberate pre-seed scoping (avoids moderation/GDPR load and a real collusion-channel risk in a PvP money game) — not a placeholder shortcut. |
| Currency picker (inside Preferences today) | `curOpen` | ❌ none | Not started | **Resolved: real feature, not a placeholder.** `OPEN_CURS`, 7 currencies. |
| Affiliate "Create Campaign" sheet (already built) | `createOpen` | ❌ none | Not started | **Resolved: same sheet, redesigned** — `newCampName`, `submitCampaign`, drag-to-dismiss, "CAMPAIGN CREATED" toast. Matches what's already built; this is a restyle to new tokens, not new logic. |
| `coinflip-hub`, `blackjack-hub`, `chess-hub` | *already matches the new design* | ❌ none | **Done — no work** | Designer-confirmed: these 3 are already finished under the new design. QA note: cross-check these against rps/mines/dice once those ship — a visible mismatch becomes its own follow-up ticket (see roster table above). |
| `crash-hub`, `roulette-hub`, `keno-hub`, `baccarat-hub`, `limbo-hub`, `hilo-hub` | *no hub rebuild — chrome only* | ❌ none | Not started | Tiles stay in the lobby; these 6 hubs get no redesign work of their own, they just need to render correctly once the shared header/bottom-nav/tokens change platform-wide. Track as a side effect of the shared-component work, not separate tickets. |
| `ships-battle-hub` | *removed from the platform* | ❌ none | **DONE** | Merged via PR #457 (2026-09-09). Code archived at tag `archive/ships-battle`. |
| `rps-hub`, `mines-hub`, `dice-hub` | `rps`, `mines`, `dice` | ❌ none (README explicitly lists these as not captured) | Not started | The only 3 games needing a real screen rebuild this round. No screenshots exist despite being named views — reachable by click-through only. |
| `lobby`, `stake-entry`, `play`, `result` | idle → run → done phases, one screen per game | ❌ none | Not started | **Resolved, and confirmed a real change, not already true today**: checked `App.tsx` directly — these are still 4 separately-rendered live screens today (`case 'stake-entry'`, `'lobby'`, `'play'`, `'result'`, `App.tsx:1292-1317`), including a real fallback path (`hubScreenFor(meta.id) ?? 'stake-entry'`) for any game not yet mapped to a hub. The new design genuinely collapses these into one screen with internal phases — a real architectural simplification to build, not a relabeling. |
| `leaderboard` (separate screen) | a tab within each game view: `TABS: Open Games / 24h Race / Weekly Race / Leaderboards` | ❌ none | Not started | **Not just a relabel — 24h Race and Weekly Race are net-new features, not built anywhere today.** These match Menu overlay's existing COMPETE group rows ("24H race," "Weekly race" — currently `action: 'placeholder'`, built speculatively a few tickets ago, never wired). Implementing this tab for real means real time-windowed leaderboard logic, not just moving the existing `leaderboard` screen's content into a tab. Size this on its own — it's materially bigger than folding lobby/stake-entry/play/result together. |
| `wallet` (standalone screen), guest mode (`guest-loading`/`guest-picker`) | *out of scope* | ❌ none | **Resolved: leave both untouched.** Confirmed by the Designer's team: "WALLET" appears once as a bare button label with nothing behind it, "Guest" appears zero times in the export. Neither is part of this redesign — current implementations stay as they are. |

## Standing decision — resolved: light theme is fully in scope, not deferred

Closes the loop every prior ticket (Account, Preferences, Menu) independently deferred. Designer's decision (2026-09-09): **build it, all the way, this round.** Theme model is `dark | light | system` (three values — `PreferencesHub.tsx:23`'s current `'dark' | 'light'` needs a third option, with "system" watching `prefers-color-scheme`). Three values fall outside the documented token table and need adding to it rather than hardcoding: sort-sheet background, theme-button shadows, and the **games-hero carousel inactive dot** (hardcoded `#3B3B47`, dark-only, no light equivalent in the file — Designer Q5, 2026-09-09). Verification: the Playwright harness generates its own light-mode reference screenshots directly from the prototype (which has a working theme picker) rather than waiting on design or relaying phone photos. Coinflip/Blackjack/Chess (already-done, not rebuilt this round) get an explicit interim rule — light everywhere except their player-boxes/board, via one scoped dark-override wrapper, never forked components — with a real light treatment for those three parked as its own follow-up once rps/mines/dice ship. Full detail: `docs/COMMS/from-advisor/light-theme-rollout.md`.

## Stop in-flight design work — RESOLVED (Designer 2026-09-09, `migration-order-of-work.md`)

Directive: halt any of this cycle's screen-correction work that the new package supersedes; keep
only genuine content fixes, drop everything purely visual.

**Reconciled against repo state: nothing is actually in flight.** Every screen-correction ticket
already merged to `main` on 2026-09-07 (#451/#454 Affiliate; #404 Account; #401 Preferences; #446
Menu; Rewards redesign + #435 rakeback). Only open PRs are #456 (deploy record) and #457 (Ships
Battle removal). So there is nothing to stop — the actionable part is extracting the content
corrections below so the rebuild carries them.

### Content corrections — RESOLVED (Designer 2026-09-09, `content-corrections-answer.md`)

**Nothing needs overriding.** The Designer checked all six candidates against
`design/prototype/RapidClash Full Spec.html` — every one is either already correct in the prototype
or superseded by the redesign:

| Candidate | Verdict |
|---|---|
| Affiliate flat 20% / no ladder | Prototype matches our build exactly (hero numeral line 1687, copy line 1694). Build as-is. |
| Affiliate `https://` link prefix | Prototype shows it (line 1699). Our #451 fix agrees. |
| Affiliate stats tiles 18 / 42,910 / 1,284 | Identical in prototype. **Match the mock values exactly** or the pixel diff fails every run. Same for campaign seeds (`bobbylee`/`KAI-9F2C`/0/20%, `winorgohome`/`winorgohome`/0/20%). |
| Affiliate promo/link heading icons (#452) | **Dropped, not carried.** New design has no heading icons — restructured into two plain-text pill rows ("LINK:" / "CODE:" + copy button each). |
| Account / Preferences / Menu content | Our read holds — nothing to carry. |
| Rewards VIP ladder | Build straight from prototype `VIP_TIERS`/`VIP_ROWS`, no override — see canonical table below. |

### ⚠ THE STALE-ARRAY RULE — applies to every screen rebuild, not just Affiliate

`RapidClash Full Spec.html` carries **dead data-layer arrays that nothing renders** but that look
authoritative next to the live values. Known ones:

| Array | Line | What it looks like | Reality |
|---|---|---|---|
| `affiliateTiers` | 4313 (`renderVals()`) | Bronze 0–50K/5% · Silver 50K–250K/8% · Gold 250K–1M/12% · Diamond 1M+/18% | Dead. Not referenced. **Do not build.** (Also ≠ the 20/25/30/35 ladder we deleted in #452 — neither is real.) |
| `affiliateSteps` | 4308 | some step list | Dead. Build the *rendered* "HOW TO GET STARTED": Step 1 Create your campaign / Step 2 Share your link or code / Step 3 Earn on every game. |
| `TIERS` | 2896 | Bronze/Silver/Gold/**Platinum**/Diamond, 5/8/12/15% rakeback | Dead. The real VIP ladder is `VIP_TIERS`/`VIP_ROWS` (below). |

**Rule: if no markup references it, it doesn't exist. Build from what's on screen, not the data
layer. Hit another orphan array — flag it, don't build it.**

### Canonical Rewards VIP ladder (`VIP_TIERS`/`VIP_ROWS`, Designer-verified against live)

| | WOOD | BRONZE | SILVER | GOLD | EMERALD | DIAMOND |
|---|---|---|---|---|---|---|
| XP required | 500 | 5,000 | 25,000 | 125,000 | 475,000 | 1,500,000 |
| Rakeback | 1% | 4% | 7% | 10% | 15% | 20% |

Weekly & 24h races: all six · Exclusive tournaments: Silver+ · Higher stake tables: Gold+ ·
Monthly volume bonus: Emerald+ · Dedicated VIP host: Diamond only.

### Affiliate Overview structural notes for the rebuild ticket (from the Designer answer)
- No card wrapper — hero/link/code sit directly on the page background (already flagged in #448).
- LINK field: `overflow-x:auto` + `white-space:nowrap` in a fixed-height pill (scrolls horizontally, no truncation). CODE row: `text-overflow:ellipsis`. Deliberate difference.
- Campaign-card short link: `rapidclash.com/r/` + first 4 chars of code + ellipsis.

### Canonical new Mines ruleset (Designer, 2026-09-11) — this is a RULES change, not a reskin

The prototype's 5×5 Mines grid is intentional and its engine is built around it (`MINES_TILES` = 25 at proto line 2863, `pickBombs()` draws 3 from 25 at line 2865, gem list caps at 22 at line 3718). Today's 8×8 / 7-mine tuning does **not** carry over. The T6c/engine ticket is built from this list:

1. **Board: 5×5, 25 tiles. 3 mines / round, 22 safe. Random placement each round.**
2. Both players get the identical board — keep the deterministic-seed contract, **parameterised for `(size, mineCount)` instead of hardcoded 8×8 / 7. Generalise the engine, do not fork it.** The prototype's `DEFAULT_BOMBS = [4, 11, 18]` is a placeholder layout, not a rule.
3. **30-second clock per round**, starting when the match begins (proto `minesClock: 30`, line 3328). Replaces today's per-player 5s move timers. **Confirmed by the Designer: this clock is a cap, not a mechanic** — it exists purely to end a round for a player who stops tapping (disconnect/distraction), not to create time pressure. A player tapping normally finishes (mine, clear, or bust) well inside 30s.
4. Tap a tile to reveal. Each safe tile = 1 gem.
5. Hit a mine → your round ends immediately, **you keep every gem collected before it** (already how today's engine works — bust locks at current safe count, never zeroes). **Designer confirmed: no change from today.**
6. Clock runs out → round ends, you keep your gems.
7. **No cash-out / stop button.** Only a mine, the clock, or clearing all 22 ends a round (see the three sub-answers below).
8. Most gems wins the pot.
9. **Equal gems → draw → rematch**: same stakes, new seeded board, same two players, nothing paid on the draw, pot carries into the rematch (already the engine's internal-draw behaviour). **Designer confirmed: keep the existing 10-consecutive-draw void cap exactly as-is** — draws are more reachable on a 22-max board than on 57, but 10-in-a-row still won't happen; it's a backstop, not a rule anyone will hit.
10. **Remove the 4s auto-reveal entirely.** Only a player tap reveals a tile. If they don't tap, nothing happens until the clock. Nothing in the engine may assume a minimum number of reveals per round.
11. Maximum score is 22.

**Designer's framing that governs the whole rewrite:** hitting a mine costs you nothing but the rest of your round — you keep your gems, the round just ends. Tapping therefore always has non-negative expected value (gain a gem, or end with what you already had) and there is never a rational reason to stop or wait. **Optimal play is simply "tap until a mine or 22."** The winner is whoever's tap order survives longer on the same board. **Build the engine on this assumption — nothing should model a player "deciding" whether to continue.**

**All five sub-questions from the rules-diff — ANSWERED 2026-09-11, ticket is now fully unblocked:**
1. **Clearing all 22 safe tiles → auto-lock at 22, same as today's "cleared" behavior.** Once the 22nd safe tile opens, every remaining tile is a mine — nothing left to play, lock immediately. Both players at 22 is a draw (rule 9 applies).
2. + 3. **Remove early resolution entirely. Opponent gems stay hidden until BOTH players' rounds are over** (mine, clock, or 22 — whichever comes first, independently for each player), then compare. This is a genuine reversal of today's engine (which resolves the instant a locked player is passed, and reveals the opponent's live count once either player locks) — not just a view-redaction tweak. **Why:** since every player taps until they bust/finish regardless, early resolution and a visible counter only leak the outcome early — they don't change anyone's play. Hidden-until-the-end also decouples the two players' online timing (useful with low concurrent players): they don't need to be live in the same instant, just both eventually finish.
4. **Keep the 10-draw void cap exactly as-is** (folded into point 9 above).
5. **Disconnect confirmed: no void, no special handling.** A dropped player locks at whatever gems they had when their 30s clock ends — including locking at **zero** if they dropped before tapping anything. Explicitly the right outcome (a disconnect-voids-the-match rule would make disconnecting an escape hatch for whoever's behind).

**Seed-security question raised by the Designer, verified — already satisfied, no code change needed for this part:** since the outcome is now decided purely by which tiles each player taps (no early peek at the opponent, no cash-out), it matters more than before that neither player can see or predict the seed before their own round ends. **Confirmed directly against `mines.ts`:** the non-terminal `viewFor` branch reconstructs `{players, round, draws, boards}` from scratch — it never spreads `state`, so `seed` cannot leak through it; a player's own `mines` array is only attached once THAT player has locked (their round is already over, so it can't help them); the only place `seed` appears is the terminal branch (`{...s, mines: [...]}`), which — once early resolution is removed per point 2/3 above — only fires once **both** players are done. The existing design already gets this right; removing early resolution actually closes the one path that could have exposed it prematurely.

**Real architecture question for whoever implements this — flagged, not solved here, because it touches shared core infrastructure other games depend on:** rule 10 ("no automatic moves… nothing happens until the clock") means Mines can no longer use the core's existing per-player-timer contract (`meta.moveTimeoutMs` + `timeoutMove`) as-is — `timeoutMove` is required to return a legal move on expiry (used today for Mines' own random-reveal, and by Blackjack/Coinflip/Limbo/Keno/Roulette/RPS for their own timeout behavior). Mines' new 30s clock needs to **lock the player with no move injected at all** — there's no existing "lock without a move" primitive in `packages/core/src/matchmaking.ts`'s generic sweep. This needs a small, generic addition to the core timer contract (not a Mines-only branch, per invariant #5) — whoever picks up the engine ticket should propose the mechanism and get it reviewed given how many other games share this file, rather than build it silently inside `mines.ts`.

## Migration plan — phased order of work (Designer-set, 2026-09-09)

Supersedes the loose "sizing candidates" list. Phases 1→3 are sequential; Phase 4 runs alongside 2.

### Phase 1 — Land the prototype as committed source of truth — **DONE (PR #458, `68fb896`)**
- `design/prototype/` is on `main`: 131 files, `assets/export/` with its 14 webp tiles, `RapidClash Full Spec.html` + runtime. Plain commit, ~37.7 MB (git-lfs offered, not taken).
- **Remaining:** asset triage — delete the ~61 unreferenced exploration PNGs, decide git-lfs-or-drop for the ~1.5 MB masters now that `assets/export/`'s webp tiles are what ship. Mechanical follow-up PR, no blocking decisions.

### Phase 2 — Playwright screenshot-diff harness — **MERGED (#459 + #466)**
- `tools/design-fidelity/` — demo/dev-only package, ADR-010 carve-out like `bot-crowd`.
- **#459** (`2d88e54`): hermetic prototype render (dc-runtime pulls React 18.3.1 from unpkg; harness serves it from the workspace); light theme via initial-state injection; `capture-prototype` → 16 committed references, chrome-clipped; `capture-app --url` → the running app; `diff` → pixelmatch %, diff images, `report.json`. Proven end-to-end.
- **#466** (`8795f59`): Designer's harness spec — fidelity gate **≤ 0.5% differing pixels** + a human look at any drift (`diff` prints PASS/FAIL); **freeze layer** (`src/freeze.ts`) pins `Math.random`/`Date.now` and strips all transitions/animations, both sides; **region masking** (`MaskRect`) — `BANNER_MASK` excludes the carried-over carousel from the 4 games screens; viewport **390 × 840**.
- **Still to do — 3rd harness PR (Advisor):** whole-page comparison in **viewport-sized scroll frames** (Designer Q3) — not blocking Games-hero (above the fold); needed before Rewards / Account rebuilds. Also: CI `playwright install chromium` if the harness is ever run in CI.

### Phase 3 — Games page hero rebuild — **MERGED (#468, `15253fe`)**
- Category rail (5 tabs, many-to-many `CATEGORY_GAMES`), SEARCH (cross-category substring), SORT (Popularity / Newest / Alphabetical), RANDOM (1560 ms spin → random among the 6 playable games), section title. Banner untouched. `getPopularity()` added to `match-history.ts` (generic `GROUP BY game_id` aggregate — invariant #5 clean) + `GET /games/popularity`.
- **Merged at fidelity FAIL** on the PM's diff-image review — every failing region is out of scope (shared header/nav chrome, tile art, no light theme, harness alignment). That's *why* shared-chrome + light-theme is next (below), not rps/mines/dice.
- **Follow-ups the harness surfaced** — a "games-grid fidelity" ticket (`ADVISOR_TO_PM.md` 2026-09-10#2, from a direct DOM measurement of the drift):
  1. **Tile aspect ratio** — `HomeHub.tsx:554` uses `aspect-[2/3]` (0.667); the prototype's tile box is `112/158` (0.709). ~12px too tall per tile, compounding down the grid — the dominant "drift grows downward" cause. Fix: `aspect-[112/158]`.
  2. **Default tile order** when Popularity is all-tied → the prototype's `GRID` order (Coinflip first), not the current tie-break (showed Baccarat first).
  3. Rail→pills vertical gap ~15px tighter than the prototype (lower priority, needs a spacing pass).
  - *(A "tile art" flag was retracted — the "100"/"1K" are chips in the Baccarat art; the webp tiles match the prototype PNGs.)*
- Scope was: category rail, filter pills (SEARCH / SORT / RANDOM), section title. NOT the banner.
- **Category rail** — `CAT_GAMES` mapping (many-to-many tags, see the category table above). `MenuOverlay.tsx`'s placeholder "Card games / Chance games / Skill games" rows get wired here.
- **Search / Sort** — behaviour RESOLVED (see "Two UI elements"): name substring across all 12, ignore tab; Popularity = all-time settled-match count; Newest = fixed intro-order ordinal; Alphabetical = display name.
- **RANDOM button** — defined intent, not ours (Designer Q4): `spinRandom` (line 3991) spins the die 1560 ms, then opens a random game's hub. Prototype only picks mines/rps/dice because those are its only views — **real behaviour: random among the six *playable* games (rps, dice, mines, coinflip, blackjack, chess)**, never the six unbuilt ones (dead hub is worse than a smaller pool). Spin keyframe is 1500 ms, nav fires 60 ms after it settles.
- **Banner carousel — DO NOT rebuild** (Designer Q5). The three rotating banners, artwork, rotation and dots are carried over from production unchanged. Harness masks the region. *One thing to do:* the inactive dot is hardcoded `#3B3B47` dark-only with no light value — add a light-theme token for it (rides with the light-theme rollout).

### Phase 4 — Remaining open decisions (parallel with Phase 2)
- **Search matching — RESOLVED** (Owner 2026-09-09): substring match on game name across all 12 games, ignore active tab, case-insensitive, as-you-type. See "Two UI elements" section.
- **Sort metrics — RESOLVED** (Owner 2026-09-09): Popularity = all-time settled-match count per game; Newest = fixed introduction-order ordinal from git history, not a live date field; Alphabetical = by display name. See "Two UI elements" section.
- **Content corrections — RESOLVED** (`content-corrections-answer.md`): nothing to override.
- **Harness + hero questions — ALL 6 ANSWERED** by the Designer 2026-09-09 (`TO-designer-harness-and-hero.md`): harness answers folded into Phase 2; Games-hero answers (RANDOM, banner) folded into Phase 3; rps/mines/dice confirmed final (see below). Nothing left held.

### After the plan — sequenced, not yet scheduled
These are scoped in their own sections/comms docs and sequence *after* the harness exists:
- **rps / mines / dice** full screen rebuilds — the only 3 games needing new screens. **Designer-confirmed final, not sketches (Q6): build them fully, gameplay included.** Each has a real phase machine (mines: idle → match → run → done, with a matchmaking phase), clocks, opponent state, seeded history, outcome logic (`rpsOutcome` win/lose/draw; mines gems/bombs/bust; dice belt animation + roll history) — ~25 state keys across the three. Reference screenshots come from the harness.
- **Currency skin + fixed stake ladder** — `$`/multi-currency wallet skin (cosmetic) + the 6-rung `$1–$100` `STAKE_LADDER` replacing free-typed stake entry. See "Currency presentation" above. The stake-ladder half touches the core/matchmaking, size it separately from the skin.
- **Shared chrome + light theme rollout** platform-wide — **NEXT workstream** (Owner-agreed 2026-09-10: before rps/mines/dice, because it's what fails fidelity on every screen). Sequenced tickets in `ADVISOR_TO_PM.md` 2026-09-10#1:
  - **T1 — Theme foundation**: `dark | light | system` model (`system` → `matchMedia`), app-wide theme provider replacing `PreferencesHub`'s subtree-only one, 3rd Preferences radio, token additions (sort-sheet bg, theme-button shadow, carousel inactive-dot light value). Gates T2–T4.
  - **T2 — Shared chrome**: rebuild `HubRibbon` (header) + `HubToolbar` (bottom nav) to the prototype + light. Highest-leverage lift (renders on every screen). Harness adds the bottom-nav to its compared region here.
  - **T3 — Per-screen light threading**: Account / Preferences / Menu / Rewards / Affiliate + HomeHub — replace each screen's local `RC = {hex}` object with the shared token set. Parallelable with T2.
  - **T4 — Interim dark-override** for Coinflip/Blackjack/Chess: one scoped `--rc-*` override wrapper per hub around the play surface; do not fork components; audit hardcoded hex inside first.
  - **T5 — PARKED**: proper light treatment for those 3, after rps/mines/dice.
  - Carries the 6 chrome-only hubs (crash/roulette/hilo/keno/baccarat/limbo) as a side effect of T2.
- **Chat** — full UI + `ChatTransport` local impl + server-checked kill switch (`chat-local-transport.md`).
- **Auth modal** replacement — `referral` field stays cosmetic/session-local (resolved).
- **Races (24h / Weekly) + Leaderboards tab** — genuine new feature work (time-windowed leaderboard logic), sized separately from the lobby/stake-entry/play/result → single-screen-with-phases collapse.
- **lobby / stake-entry / play / result → one screen, internal phases** — real architectural simplification (`App.tsx:1292-1317` today).

## Status snapshot — 2026-09-24 (D01-D41 + 2026-09-18#1/#2 + 2026-09-21#8/#9/#10/#11 + 2026-09-22#5/#6/#7 + 2026-09-24#1/#3 (service-worker skipWaiting/clientsClaim; AudioContext resume-on-visibility; WS reconnect socket leak; periodic ledger cleanup; VACUUM) ALL shipped, merged, AND deployed live at `rapidclash-00132-z76`, all independently verified — the live snapshot is now confirmed 48.72MiB, down from 372MiB; the GCS snapshot bucket's own object versioning (428.74GiB / 5,849 retained versions, unrelated code path) found and cleaned up directly, bucket now 1 object, versioning disabled; a Blackjack RNG fairness question (2026-09-21#7) was investigated and closed with NO bug found; NEW — 2026-09-24#2 (silent escrow-orphan race + Owner-proposed self-heal check), Owner-approved, queued for tomorrow per Owner's own explicit no-rush instruction) — supersedes all earlier snapshots in this section

**2026-09-24#3 — shipped in PR #704, independently re-verified against the diff, re-tested (targeted 40/40, full apps/server+packages/core suite 457/457), AND the LIVE deployment, deployed live at `rapidclash-00132-z76`. VACUUM the live DB after any cleanup pass that deletes rows, plus once at startup if pre-existing bloat is found — 2026-09-24#1's DELETE alone didn't shrink the file. Full detail: `ADVISOR_TO_PM.md` 2026-09-24#3.**

- **Confirmed directly, not assumed: the live snapshot is still ~390MB after tonight's 654K-row backlog delete.** Standard SQLite behavior (freed pages become reusable space WITHIN the file, never returned to the OS) — nothing in this codebase sets `auto_vacuum`, checked directly, so it's off by default. Means 2026-09-24#1's actual goal (cheaper `db.backup()`-based snapshot uploads) isn't fully realized yet.
- **Tested the real fix on a real copy of the actual production data before proposing it.** Replicated the exact same delete rule locally: 327,433 eligible groups, 655,190 rows deleted, closely matching PM's own live numbers — took 118 seconds (genuinely substantial SQL work touching 5 indexes across 655K rows, not primarily a CPU-throttling artifact as first guessed). Then `VACUUM` on the result: **1.05 seconds, 389.5MB → 49.85MB (87% reduction)** — fast because it only rewrites the surviving ~100K rows, not the original bloat.
- **Stays cheap indefinitely, not just for tonight's one-time backlog.** With the hourly cleanup now running, the DB never again accumulates anywhere near 655K stale rows before pruning — every future VACUUM operates on a small delta, staying fast.
- **Two call sites, reusing 2026-09-24#1's own shape exactly:** unconditionally at startup (reclaims tonight's already-existing bloat, since the backlog is already cleared and a "only if rows deleted" gate alone would never fix it), and after the hourly pass gated the same way `onWrite` already is (`rowsDeleted > 0`). A real, un-chunkable blocking operation, but bounded — 1 second measured, not the open-ended risk a raw VACUUM-of-a-bloated-file would carry.

**PM's implementation, independently verified against the diff — a well-justified improvement on the ticket's own literal wording, not a workaround.** The ticket asked for VACUUM "unconditionally at startup"; PM instead gated the startup call on `PRAGMA freelist_count > 0` (the real, direct signal for "does this file actually have reclaimable dead space"), correctly avoiding a wasted VACUUM+`onWrite` on every future clean boot once the file is already compact — a genuinely more precise implementation of the ticket's actual intent, surfaced by 2 existing onWrite-count tests (`admin.test.ts`, `auth.test.ts`) that would otherwise have broken. The hourly timer's own pass stays gated on `rowsDeleted > 0` only, correctly relying on each tick's own VACUUM having already kept the file compact between ticks. Both new tests verify the real `PRAGMA freelist_count` directly, not just "some function ran." Ran the targeted tests myself (`ledger-cleanup.gateway.test.ts` + `admin.test.ts` + `auth.test.ts`): 40/40. Full `apps/server`+`packages/core` suite: 457/457, clean.

**Deployed live at `rapidclash-00132-z76` — independently re-confirmed directly against production, numbers matching exactly.** Confirmed via `gcloud`: revision serving 100% traffic, zero errors/429s since deploy (only the expected benign 409s from the bot-crowd restart), `/open-challenges` live. Fetched the exact log sequence myself: `"pruned 180 match(es), 360 row(s)"` at `00:04:22.324Z`, `"VACUUM complete"` at `00:04:23.044Z` — a ~720ms gap, matching PM's own "fast this time" report. Checked the live GCS object size directly: **51,089,408 bytes = 48.73 MiB**, matching PM's reported 48.72 MiB almost exactly, down from 372.22 MiB before this fix — the 87% reduction predicted by the local test held up precisely against the real production file.

All 4 of tonight's tickets (2026-09-22#7's WS reconnect fix, 2026-09-24#1's ledger cleanup, and now 2026-09-24#3's VACUUM) are shipped, merged, deployed live, and independently verified end to end. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket. 2026-09-24#2 (the escrow-orphan race + self-heal check) remains queued for tomorrow per Owner's own explicit instruction — the only thing still open.

---

**2026-09-24#2 — NEW, ready to ticket, queued for tomorrow (Owner: no rush). Root cause of the 1,997 historically-stuck `BET_ESCROW` rows found — a silent race between the sweep and the socket-close cleanup — plus Owner's own proposed self-healing check, refined into a precise safe rule. Full detail: `ADVISOR_TO_PM.md` 2026-09-24#2.**

- **The mystery, resolved end to end.** 97 tight clusters account for 1,996 of 1,997 stuck cases (99.95%) — one recurring event, not scattered noise. That event: Cloud Run's own 60-minute hard WS timeout force-closing the entire bot-crowd roster near-simultaneously (already documented in `tools/bot-crowd/src/bot.ts`'s own code comment) — confirmed directly via the demo-taker VM's own systemd journal (SSH): 14 bots logged "socket closed" in the same second on 2026-09-02, matching a stuck cluster exactly, with the SAME Cloud Run `instanceId` throughout (not a deploy, not a crash — a routine platform timeout).
- **Traced one account's exact 3-escrow sequence to find precisely where the loss happens.** `🤖@snakeeyes`: socket force-closed → correctly auto-refunded at that instant; reconnects, reposts, refunds normally 91s later via the sweep; reposts again 4s later — **never refunded, ever.** Two clean cycles, then silence on the third, repeated per-bot across every cluster.
- **Where it's lost: confirmed by the total absence of any corroborating log, not a captured stack trace.** `sweepExpired`/`leaveQueue` (`matchmaking.ts`) both remove-from-index BEFORE refunding — if the sweep and the socket-close cleanup ever touch the same entry close together (plausible during a mass-reconnect burst), whichever runs second finds it already gone and throws. The sweep's own version of this failure IS logged — zero hits ever, searched the full retained period. The socket-close handler's copy of the SAME race (`gateway.ts:747-756`) has NO logging at all — the exact, structural reason this stayed invisible for 3 months.
- **Fix, part 1: stop the silence, remove the race.** Log the empty catch; harden `leaveQueue`/`sweepExpired` to check-then-act instead of act-then-check.
- **Fix, part 2: Owner's own proposed self-heal, refined into a safe rule.** Before a new bet, reconcile any of the account's OWN unsettled escrows whose `match_id` exists in NEITHER `matches` NOR `entryByMatchId` — the two in-memory maps that are the actual source of truth for "still legitimately active." A naive "any unsettled escrow" version would risk refunding a live match out from under someone; this version can't. More robust than hardening the two racing paths alone — works directly off the durable ledger, so it'd catch even a future, different bug that drops an in-memory entry. A complement to Fix 1, not a replacement — ship both together.
- **Impact, quantified before proposing anything:** 1,997 of 2,002 total stuck escrows are bots (trivial, self-heals via auto-top-up). **5 are real human testers** (`iajsjz`, `ijh`, `DemoGM`, `Bobbylee`), 500 total stuck — confirmed NOT locked out of further betting (`hasOpenEscrow` doesn't currently gate `joinQueue`/`takeChallenge`).

**Advisor next:** available, no open thread. **PM next:** queue for tomorrow per Owner's own explicit instruction — nothing here is time-sensitive, every cluster found is already historical.

---

**2026-09-24#1 — shipped in PR #702, independently re-verified against the diff, re-tested (ledger+gateway tests 31/31, full apps/server+packages/core suite 455/455), AND the LIVE deployment, deployed live at `rapidclash-00131-kbc`. A periodic ledger-cleanup pass purging fully-resolved-as-cancelled BET_ESCROW/SETTLE_REFUND pairs older than 2 days — root cause chain from 2026-09-22#7 traced one level deeper. Full detail: `ADVISOR_TO_PM.md` 2026-09-24#1.**

- **The live DB's own 371MB size (driving the 2026-09-22#7 snapshot-upload cost) is nearly all `ledger_entry`: 755K+ rows for only 8,751 real completed matches.** Verified directly against a full downloaded copy of the production snapshot, not inferred. Separately: the GCS bucket holding that snapshot had its own unrelated problem — object versioning with no lifecycle rule had silently retained 5,849 old versions / 428.74GiB since 2026-06-29. Per Owner's explicit instruction, cleaned up directly (not a PM ticket — a one-time bucket action): versioning disabled, every noncurrent generation deleted, bucket now holds exactly the 1 live object. Documented in `docs/DEPLOY.md`.
- **Traced the `ledger_entry` growth to its exact mechanism, verified against real timestamped data, not assumed.** 359,358 of the rows are bot "resters" (ADR-010) posting an open challenge, having it expire unclaimed ~90s later (the game's own pick-window TTL), and reposting 4s after that (`repostDelayMs`) — forever, every ~95s, per resting bot, since 2026-08-21. Confirmed via one bot account's own full history: `BET_ESCROW → SETTLE_REFUND (+90.9s) → BET_ESCROW (+4.08s) → ...`, repeating exactly. This is ADR-010 working as designed (a human should always have something to join) — nothing was ever wrong with the escrow/refund logic itself; nothing ever prunes the resulting dead rows once they're no longer relevant.
- **The exact safe-to-delete rule, verified against every real type-signature in the data, not just the common case.** Grouping `ledger_entry` by `match_id`'s own row-type set found 2 genuinely-meaningless signatures (`BET_ESCROW,SETTLE_REFUND` ×359,196; `BET_ESCROW×2,SETTLE_REFUND×2` ×162 — both sides refunded, a cancelled match) and 2 genuine-match signatures to leave untouched (`BET_ESCROW×2,SETTLE_WIN,RAKE` ×8,216; `BET_ESCROW×2,SETTLE_WIN` ×373). Critically also found 2 ambiguous signatures (`BET_ESCROW` alone ×1,992; `BET_ESCROW×2` alone ×5 — still-open or stuck) that a naive "match_ids with only 2 rows" rule would have wrongly swept up — the correct rule requires at least one actual `SETTLE_REFUND` present, which these lack, correctly excluding them. Owner's own explicit instruction: leave these for a separate investigation, out of scope here.
- **Cadence: Owner's own two proposed triggers (on bet-placement activity, or "natural request serialization") were both identified as re-creating the exact 2026-09-22#7 bug in a different spot** — `better-sqlite3` is synchronous, so a large `DELETE` running inline with an ordinary bet request would block the whole single-threaded server for its duration, same mechanism as the snapshot-upload incident. Proposed instead: an independent hourly timer in the exact `setInterval(...).unref()` idiom `gateway.ts` already uses for the heartbeat/sweep timers, with each run batched (bounded chunk size, yielding via `setImmediate` between batches) so no single pass can ever block for long regardless of backlog size — the identical chunking pattern already proven safe in `better-sqlite3`'s own `backup()` API (read directly from its source for `2026-09-22#5`).
- **The one-time backlog (~1 month, 359K+ rows) is handled by running the same batched pass once at server startup, not a separate manual step — deploying tonight IS the one-time cleanup.** Owner has explicitly confirmed tonight (off-hours, no active testers) as a safe window for that first, larger pass.
- **Retention as an env var** (`LEDGER_CLEANUP_RETENTION_DAYS`, default 2 — Owner's own number), matching the codebase's existing tunable-constant convention. A real cleanup pass deliberately triggers the existing debounced snapshot too, so the next restart restores the smaller file.

**PM's implementation, independently verified against the diff — matches exactly.** `cleanupSettled(retentionDays, batchSize)` on the `Ledger` interface implements the exact signature/age-cutoff rule verbatim, batched DELETE with a genuine `setImmediate` yield between (not after) chunks, the independent hourly timer wired correctly (never bet-activity-triggered, per the explicit warning), `onWrite` fired only when rows are actually deleted, `EphemeralLedger` gets a documented no-op. PM's own extra diligence — checking `match-history.ts`'s `matchNetStmt` for safety against pruning a genuine draw — independently re-verified directly against the source: `SUM(amount)` over the real rows and `NULL ?? 0` after they're gone both resolve to exactly 0 for a draw; correct either way. Ran the tests myself: 31/31 on the new ledger/gateway suite, 455/455 on the full `apps/server`+`packages/core` run — clean. One discrepancy caught and corrected: PM's own report named a "DiceHub flake" for CI's one failure, but CI actually failed on a different, unrelated pre-existing timing flake (`heartbeat.gateway.test.ts`) — confirmed via a standalone re-run (2/2, 522ms) and confirmed this PR never touches the heartbeat code at all.

**Deployed live at `rapidclash-00131-kbc` — independently re-confirmed directly against production.** The real backlog pass ran and completed at boot: `"[gateway] ledger cleanup: pruned 327013 match(es), 654350 row(s)"`, confirmed in the live logs exactly as reported. Zero 429s/5xx since deploy (only the expected benign 409s from the bot-crowd restart). One real gap caught during verification, not before shipping: the DELETE correctly removed the rows, but the live snapshot's own byte size didn't shrink (SQLite frees pages for reuse, doesn't return them to the OS without a VACUUM) — tracked as its own follow-up, `2026-09-24#3`, rather than silently treated as fully resolved.

D01-D41 plus every 2026-09-18/2026-09-21/2026-09-22/2026-09-24#1 ticket are now shipped, merged, AND deployed live at `rapidclash-00131-kbc`. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-22#7 — shipped in PR #698, independently re-verified against the diff, re-tested (WS suite 35/35), AND the LIVE deployment through a real stress test, deployed live at `rapidclash-00130-pnb`. Owner's own live report: "we currently experience some lagging of the demo game." Full detail: `ADVISOR_TO_PM.md` 2026-09-22#7.**

- **Confirmed live right now, not just historical.** `gcloud logging read`: 42 HTTP 429s ("no available instance") in the last 15 minutes, accelerating (24 in one second at one point). `gcloud run services describe`: `containerConcurrency: 300`, but `maxScale: '1'` — this service is pinned to exactly ONE instance, 1 vCPU/1Gi, and structurally cannot scale past it. Every 429 right now is that one instance's 300-slot ceiling being hit.
- **Root cause: a reconnect overwrites the OLD socket out of the connection map without closing it, and the 2026-09-21#9 heartbeat structurally cannot see it once that happens.** `gateway.ts:663`, `connections.set(playerId, socket)` — unconditional overwrite, old socket never explicitly closed. The heartbeat (`:622`) only iterates `connections.values()` — sockets CURRENTLY in the map — so the instant a reconnect overwrites the old entry, that old socket falls out of the heartbeat's reach permanently, not just for one tick.
- **The codebase's own comment on the heartbeat states the exact design intent this defeats:** "freed within ~30-60s, instead of sitting occupied for up to Cloud Run's own 3600s hard cap" (`:161-165`). For every reconnect-orphaned socket, that's precisely what happens instead — a 60-100x blowup versus the heartbeat's own designed guarantee.
- **Precisely matches Owner's own earlier diagnosis of the "missing tiles on reload" bug**, confirmed correct then and confirmed still true on a direct re-read of today's code — that conversation never resulted in a formal ticket; this is that fix, now.
- **Why today specifically: `2026-09-22#5` (shipped ~2 hours ago) means an open tab now auto-reloads on a new deploy — new behavior as of today.** `/ws` upgrade (HTTP 101) counts over the last 4 hours show clear bursts of 30-60 at a time (consistent with the bot-crowd's scripted restart-after-deploy cycle), each burst a mass-reconnect event that can leak sockets if the old ones don't tear down cleanly first.
- **Fix: explicitly `terminate()` the previous socket for a reconnecting `playerId`, AFTER updating the map** — a 3-line change at the one registration site (`gateway.ts:663`), reusing the exact `terminate()` → `close` → existing-cleanup-path mechanism the heartbeat itself already relies on one function up. Ordering is deliberately safe: the old socket's own close handler sees the map already pointing at the new socket and correctly takes the existing stale-close early-return (`:705`) — no bogus forfeit, no accidental deletion of the live connection.
- **Scope: `gateway.ts`-only, one call site.** Not touching `containerConcurrency`/`maxScale` in this ticket — flagging `maxScale: 1` as a fact worth Owner's own awareness, not proposing a change to it here, same standing practice as the concurrency question on `2026-09-21#11`.

**PM's implementation, independently verified against the diff — matches exactly, character for character.** The exact 3-line fix (capture previous socket, update the map, terminate the previous socket AFTER) landed verbatim at `gateway.ts:663`, well-commented with the same reasoning. New dedicated test file proves both halves precisely: the previous socket now genuinely terminates on reconnect instead of sitting open forever (PM reported verifying this one catches the regression via revert-confirm-restore), AND the stale-close guard ordering is safe — no incorrect forfeit fires on the reconnecting player's live match, which completes normally over the new socket end-to-end. Ran the full `apps/server/src/ws/` suite myself: 35/35, including the pre-existing S8 reconnect and D36 heartbeat coverage — no regressions.

**Deployed live at `rapidclash-00130-pnb` — independently re-confirmed directly against production, through a real stress test, not just a quiet deploy.** Confirmed via `gcloud`: `rapidclash-00130-pnb` serving 100% traffic. PM's bot-crowd restart after deploy is itself the exact reconnect-burst scenario that triggered the leak — a genuine live stress test of the fix, not a synthetic one. Independently checked Cloud Logging for that window myself: 62 429s in the ~29 minutes immediately before the deploy, **zero since, including through the restart itself.** `/open-challenges` live and correct.

**On Owner's own question about raising `containerConcurrency` from 300 to 600:** pulled real Cloud Monitoring CPU/memory data before recommending against doing so ahead of this fix — both were already under real pressure during the leak (CPU p99 up to ~73%, memory p99 up to ~81%, on a `maxScale: 1` instance that can't add compute), meaning widening the cap right then would have let more work pile onto an already-strained single vCPU/1Gi instead of fixing the actual problem. Post-fix, CPU has settled back to a normal range; one early memory reading through the immediate deploy/restart moment was still elevated, but that's too early and too thin a sample (a brand-new revision's own cold start, sampled a minute after boot) to read as a steady-state signal either way — flagging as needing a longer, settled observation window before treating it as informative, not as evidence for or against 600 yet.

**Advisor next:** watching for a settled post-fix CPU/memory baseline before giving Owner a grounded answer on the 600-concurrency question; no other open thread. **PM next:** nothing pending on this ticket — continuing the hour-long recurrence watch already in progress per the original ask.

---

**2026-09-22#6 — shipped in PR #694, independently re-verified against the diff, re-tested (`sound.test.ts` 7/7), AND the LIVE production bundle, deployed live at `rapidclash-00129-pr7`. A second, separate sound bug surfaced by Owner's own post-SW-fix re-test: the shared AudioContext never recovers after a mobile browser suspends it. Full detail: `ADVISOR_TO_PM.md` 2026-09-22#6.**

- **The mechanism, confirmed directly in the code.** `unlock()`/`context.resume()` — the only thing that ever revives a suspended `AudioContext` — fires exactly once per page load. `installUnlockOnFirstGesture()` installs a global gesture listener that calls `unlock()` once, then IMMEDIATELY REMOVES ITSELF. `play(name)` silently no-ops if the context isn't `'running'`, by design (never force autoplay). Combined: the first time a mobile browser suspends the context after that initial unlock — routine, expected browser behavior (screen lock, backgrounding) — sound goes silently, permanently dead for the rest of that session; no later gesture can revive it, because the one listener that could have already removed itself.
- **Precisely matches every detail of the re-test, not just loosely.** Chess/Mines sound still silent after the SW fix = a genuinely separate bug, correctly surfaced by the re-test. "Play a Friend" (a universal, purely client-side trigger, no server round-trip) ALSO silent = strong confirmation this is AudioContext-level, not Mines/Chess-specific logic. Sound "catching up" at the exact screen-unlock moment = consistent with the browser's own visibility-return behavior interacting with the suspended context and/or a backlog of game-state updates finally processing.
- **Checked the second tester's "no explosion" report for a separate root cause — found none, consistent with the same bug.** Re-verified `mines.ts`'s `viewFor` doesn't have a D31-style gap for `bustedOn` (it's a native field on the raw board object, survives both branches for free, unlike `score`). Re-verified the D39 sound-trigger condition is correct on direct re-read. Checked `mines-mine.mp3` itself for a codec/corruption issue — standard MPEG Layer III, same format as every other working sound asset. With all three ruled out, the most likely explanation is the SAME AudioContext bug — which sound gets silently dropped depends entirely on timing, so "gems worked, explosion didn't" for one round is exactly the inconsistent-per-sound symptom this bug produces, not evidence of a mine-specific code gap.
- **Fix:** listen for the page regaining visibility (`document.visibilitychange`, checking `document.visibilityState === 'visible'`) and call `unlock()` again — already idempotent and safe to call repeatedly per its own existing doc comment. The browser's gesture requirement applies to the FIRST-ever `resume()`; once genuinely unlocked once by a real gesture, subsequent `resume()` calls from non-gesture contexts are standardly permitted — a well-established pattern, not a novel workaround.
- **Scope: `lib/sound.ts`-only.** No changes needed to Mines' D39 logic, Chess's existing wiring, or anything server-side — one new listener covers every sound in the app uniformly.

**PM's implementation, independently verified against the diff — matches exactly.** A permanent `document.addEventListener('visibilitychange', ...)` (unlike the one-time gesture listener, never removed) installed right inside `installUnlockOnFirstGesture()`, calling `unlock()` on `visibilityState === 'visible'` — verbatim as specified, well-commented with the same reasoning. New `sound.test.ts` coverage is genuinely strong: one test proves the COMPLETE lifecycle end to end (initial gesture unlock → sound plays → context gets re-suspended → `play()` correctly silent → `visibilitychange` fires → context revives WITHOUT any new tap → sound plays again), the exact regression this needs to catch, not a superficial check; a second test confirms a transition TO hidden correctly does NOT attempt a spurious `resume()`. PM reported verifying the "revived" test genuinely catches the regression via revert-confirm-restore. Ran it myself: `sound.test.ts` 7/7.

**Deployed live at `rapidclash-00129-pr7` after PM confirmed with Owner — independently re-confirmed directly against production, not just the merge.** Confirmed via `gcloud`: `rapidclash-00129-pr7` is the latest revision, serving 100% traffic. Checked Cloud Logging for the post-deploy window: zero HTTP 429s; the only 4xx activity was 32× 409 on `/auth/register` in the first minute, consistent with the bot-crowd restart's own bots re-registering already-existing accounts (benign, not a deploy problem) — and confirmed `/open-challenges` is live with real bot entries. Went further than the diff/test evidence alone: fetched the actual live served JS bundle (`/assets/index-BuyBwUlO.js`) and grepped it directly — `document.addEventListener("visibilitychange",()=>{document.visibilityState==="visible"&&Bb()})` is genuinely present in the shipped code, positioned right after the gesture-listener setup exactly as designed (`Bb` being the minified `unlock`) — the fix is live in what's actually served, not just merged to `main`.

D01-D41 plus every 2026-09-18/2026-09-21/2026-09-22 ticket, including this one, are now shipped, merged, AND deployed live at `rapidclash-00129-pr7`. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-22#5 — shipped in PR #690, independently re-verified against the diff, the generated build output, AND the LIVE production deployment, deployed live at `rapidclash-00128-8lw`. Owner's own direct report: intermittent missing sound (Mines and Chess), temporarily fixed by fully closing and reopening the browser. Full detail: `ADVISOR_TO_PM.md` 2026-09-22#5.**

- **Confirmed the mechanism precisely, not inferred from behavior alone.** Rebuilt `apps/web` fresh and read the literal generated `dist/sw.js`: `index.html` and the entire JS/CSS bundle are precached and served via a `NavigationRoute` bound directly to the service worker's own cache — every page load is answered without ever touching the network once installed. This is why `2026-09-21#11`'s earlier cache-header fix doesn't help here — that fix correctly addressed the HTTP-caching layer, but a `NavigationRoute` intercepts requests before they'd ever reach that layer at all. Two separate caching systems; only one was ever touched.
- **The real gap: nothing in the current setup ever tells a newly-downloaded version to take over.** `main.tsx`'s own comment claims "automatic skip-waiting + reload... no user prompt needed" — traced this through all 3 layers involved (the app's own `registerSW()` call, `vite-plugin-pwa@0.21.2`'s own generated client template, and `workbox-window@7.4.1`'s own `Workbox` class, all read directly from the installed packages) and confirmed: none of them ever call the skip-waiting method in `autoUpdate` mode. A new service worker downloads, installs, and sits `waiting` indefinitely — the only thing that has ever activated it is the browser's own native fallback: every tab closing, then reopening.
- **Precisely explains every symptom reported.** Closing the browser = the only path that has ever actually worked. Breaking again later = another deploy landed, leaving a fresh version stuck waiting again. Mines sound missing specifically = D39 (today's batch) is what added it — a pre-D39 cached version simply doesn't have that code. Chess sound inconsistently missing too = consistent with the same device being pinned to different stale versions across different visits, given how many deploys have shipped today alone. Designer's own separate "older-looking code" reports = the same mechanism, independent corroboration.
- **Fix: `skipWaiting: true, clientsClaim: true` in the `workbox` options passed to `VitePWA()`** (`vite.config.ts:52-61`) — the standard, well-known correction for this exact class of gap. Makes the generated service worker itself unconditionally activate a new version as soon as it finishes installing, rather than depending on a message handshake nothing currently sends. Confirmed this correctly completes the app's EXISTING reload-on-update logic (the `"activated"` listener already in `main.tsx`) rather than needing new wiring — a genuine bug fix completing already-stated intent, not new scope.
- **One real tradeoff flagged, not silently decided — Owner already aware and testing in parallel.** An already-open tab could now reload automatically once a new version is detected, including mid-match. The app's own documented `match.resume` reconnect flow should handle this gracefully (same mechanism already relied on for the WS 60-minute timeout), but it's a genuine behavior change worth Owner's/Designer's own awareness, not buried silently in the fix.
- **Secondary, optional, not blocking:** the hourly `registration.update()` polling interval (`main.tsx:31`) means even post-fix, an already-open tab might not detect a new version for up to 60 minutes — worth considering shortening given how often this app deploys, but a separate question from the core fix.

**PM's implementation, independently verified against the diff AND the actual generated build output — matches exactly.** The 2-line config addition (`skipWaiting: true, clientsClaim: true`) landed verbatim in `vite.config.ts`'s `workbox` block, well-commented, no unrelated changes. Didn't stop at reviewing the diff — pulled PM's own branch, swapped just that one file into a working local checkout, and rebuilt the app myself: the generated `dist/sw.js` now reads `self.skipWaiting(),s.clientsClaim()` unconditionally at the very top of the module body, BEFORE `precacheAndRoute` even runs — confirmed by direct comparison against the OLD generated output from earlier today, which instead gated `skipWaiting()` behind a `message` listener nothing in the app ever actually triggered. PM independently confirmed the same thing (before and after syncing to latest main); this is now confirmed from two independent rebuilds, not just one. PR description flagged the mid-match-reload tradeoff explicitly and prominently, not buried in the diff, exactly as asked. No test suite coverage needed or added — a build-config-only change with no app-code surface; the generated-output inspection IS the correct verification method here, and both PM and I did it independently.

**Deployed live at `rapidclash-00128-8lw` after Owner's own explicit signal (a separate one from the D38-D41 batch, per the tradeoff) — independently re-confirmed directly against production, not just the build output.** Fetched the ACTUAL live `sw.js` straight from the deployed URL: `self.skipWaiting()`/`clientsClaim()` both present, unconditional — the fix is genuinely live, not just merged. Also confirmed `sw.js`'s own `Cache-Control: max-age=0` is still correctly in place (the D21/D36 cache-header fix's own exclusion holding on this new revision too), zero HTTP 429s in the post-deploy window, and `/open-challenges` returning live bot entries after the bot-crowd restart.

D01-D41 plus every 2026-09-18/2026-09-21/2026-09-22 ticket, including this one, are now shipped, merged, AND deployed live at `rapidclash-00128-8lw`. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-22#4 — shipped in PR #686, independently re-verified against the diff and re-tested (RpsHub 32/32), deployed live at `rapidclash-00127-l4z`. Designer's D41: RPS pick tiles have unwanted ROCK/PAPER/SCISSORS text labels, and the selection-green treatment is incomplete. Full detail: `ADVISOR_TO_PM.md` 2026-09-22#4.**

- **Part 1 (labels) — confirmed exactly as reported, in both places this renders.** `RPS_CHOICES`'s `label` field renders under the icon in both the idle preview (`RpsHub.tsx:252-256`) and the live in-match picker (`:522-539`) — the prototype's own markup is genuinely just one `<svg>`, no text. Delete both spans; `label` itself can stay (still used for `aria-label` accessibility, sound practice independent of the citation). One additional gap found while checking the same source block, not in D41's own list: our tiles are missing the citation's own 6px horizontal padding (`px-1.5`) — doesn't affect the 86px height claim, worth adding while touching this code anyway.
- **Part 2 (selection green) — the reported ring gap confirmed, plus 2 more found by reading the actual CSS values precisely, not just checking presence.** Already correct: the selected ring already uses `var(--rc-green)`, confirmed resolving to the exact cited hex pair. Gap 1 (matches D41): unselected ring uses literal `transparent` (`rgba(0,0,0,0)`) instead of the color-matched `rgba(52,211,153,0)` D41 cites — affects transition smoothness specifically. Gap 2 (found independently): current easing is `ease-out`, D41 cites plain `ease` — a genuinely different curve.
- **Gap 3 (icon colour) — confirmed NOT implemented, AND a real subtlety worth flagging before recommending the fix.** Checked the prototype's own full source precisely: `rpsChoice()` (`Full Spec.html:3244-3256`) DOES compute `color: on ? '#FFFFFF' : 'var(--rc-muted)'`, matching D41's cited values exactly — but a complete read of the picker markup shows this field is NEVER referenced anywhere; every icon fill there is a hardcoded literal. **This is dead code in the prototype's own source, not a rendered behavior.** Recommending implementing it anyway — too precise and deliberately grouped with the genuinely-rendered ring values to be an accidental citation — flagged plainly as a deliberate departure from the prototype's own literal rendering, applying as directed.
- **Implementation note:** since `RpsHandIcon` uses multiple literal fills (not `currentColor`), implementing this means the icon becomes a flat single-tone silhouette when selected/unselected in the PICKER specifically — a real visible change, not a quiet prop tweak — scoped only to that one of the icon's 4 call sites, the other 3 (idle preview, 2 reveal-card usages) keep their full multi-tone look, matching the prototype's own scoping of `.color` to the picker context alone.

**PM's implementation, independently verified against the diff — matches exactly on all 3 parts, and agrees precisely with the Gap 3 judgment call.** Labels removed at both usages, `gap-1` dropped, `px-1.5` added. Ring: `rgba(52,211,153,0)` unselected, `ease` easing, both exact. Icon-colour: `RpsHandIcon` gained an optional `color` prop (each path's `fill`/rock's `stroke` falling back to its original literal when omitted), applied ONLY at the live picker's own call site (confirmed via grep — the other 3 usages pass no `color`, keeping their full multi-tone look verbatim) — a sensible, unprompted implementation detail worth noting: the 0.42/0.72 partial-opacity accents on certain paths correctly drop to full opacity when flattened to a single color (avoiding a faint gray artifact a naive port would have left in). New 3-test suite covers all 3 parts precisely, including a direct DOM assertion that the idle preview's icon keeps its original literal fill. Ran it myself: `RpsHub.test.tsx` 32/32.

D01-D41 plus every 2026-09-18/2026-09-21/2026-09-22 ticket are now shipped, merged, AND deployed live at `rapidclash-00127-l4z`, independently confirmed (zero 429s out of 113 requests since deploy, `/open-challenges` live and correct). **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-22#3 — shipped in PR #685, independently re-verified against the diff and re-tested, deployed live at `rapidclash-00127-l4z`. Designer's D40: the opponent's gem strip wraps 3 rows instead of ~2, squeezed to roughly a third of the bar's width. Full detail: `ADVISOR_TO_PM.md` 2026-09-22#3.**

- **Confirmed exactly as reported, root cause found: a direct regression from my own D37 fix yesterday.** `GameHub.tsx:1176-1181`'s "Playing…" label (shared across every hub) went from a conditionally-UNMOUNTED element (pre-D37) to an always-mounted, in-flow (`shrink-0`) flex sibling — even at `opacity:0` (correctly the case at reveal time), it still reserves its own natural width, squeezing the gem strip's own `flex-1` sibling down to roughly a third of the bar.
- **Flagged plainly: I predicted this exact risk in the D37 ticket itself** ("switching from conditional-mount to always-mounted+opacity means the label's own box now reserves its natural width even while invisible... likely negligible... worth a live-page look rather than assuming") — that assessment was wrong; it's the entire bug D40 reports. Correcting the record, not treating this as an unattributed fresh issue.
- **A precise correction to Designer's own diagnosis: only 1 of the 3 listed causes is actually missing.** Checked each directly: point 2 (height cap, `max-height:38px`+`overflow:hidden`) is ALREADY correctly present in `GemCountRow`; point 3 (alignment, `align-content:center` present, `justify-content:flex-start` is flexbox's own unoverridden default) is ALSO already correct. The partial third row and any perceived misalignment in the screenshot are downstream CONSEQUENCES of point 1's width squeeze, not independently-missing implementations — fixing width alone resolves all three symptoms.
- **Fix reuses an existing, already-correct sibling pattern one element above it in the same file.** The prototype's own full source (line 461, not just the half D40 quotes) shows "Searching…" and "Playing…" stacked in the literal SAME absolutely-positioned slot (`position:absolute; right:14px; top:50%; transform:translateY(-50%)`), mutually exclusive by opacity. Our own code already implements exactly this for "Searching…" (`GameHub.tsx:1138-1142`, `className="absolute right-3.5 top-1/2 -translate-y-1/2 ..."`) — the fix is giving "Playing…" the identical positioning classes, replacing `shrink-0`. Parent is already `relative`, no new setup needed.
- **Confirmed safe for every hub sharing this label, not just Mines** — the label is already temporally mutually exclusive with any gem-row content (D37 already established a 560ms+ margin), and for hubs without a gem row this is a strictly more-correct change too (the name span's own growth is currently slightly squeezed by this same reservation everywhere, not just Mines).

**PM's implementation, independently verified against the diff — matches exactly, character for character.** `className` changed from `shrink-0` to `absolute right-3.5 top-1/2 -translate-y-1/2` verbatim, `GemCountRow` correctly left untouched. New test asserts the className directly (`absolute` present, `shrink-0` absent) — a precise, hard-to-misread regression guard.

**Advisor next:** available, no open thread — now live at `rapidclash-00127-l4z`, a live-page look at the opponent gem strip's actual wrapping would be worth a spot-check whenever convenient, given this touches shared code across every hub. **PM next:** nothing pending on this ticket.

---

**2026-09-22#2 — shipped in PR #684, independently re-verified against the diff and re-tested (MinesHub 36/36), deployed live at `rapidclash-00127-l4z`. Designer's D39: two new Mines tile-reveal sounds (`mines-gem.mp3`, `mines-mine.mp3`). Full detail: `ADVISOR_TO_PM.md` 2026-09-22#2.**

- **Our sound system is architecturally different from the prototype's `sfx()` — checked directly.** Ours (`lib/sound.ts`) uses Web Audio's `AudioBufferSourceNode` — every `play(name)` call spins up an independent, one-shot buffer-source from an already-decoded shared buffer, not the prototype's own single reused, rewindable `<audio>` element.
- **This makes 2 of the ticket's own "Two implementation details" already true for free.** "Overlap on fast gem taps" needs no pool — independent buffer-sources already overlap fully, nothing to cut short. "Preload on Play" already happens, earlier and more broadly — `preloadSounds()` decodes every `MANIFEST` sound uniformly, triggered by a GLOBAL first-gesture listener the instant the user taps anything on the page, well before they'd ever reach Mines' own Play button. Mute is already correctly wired (`play()`'s own `if (muted) return;`) — no separate toggle needed either.
- **The one real subtlety: the ticket's own "hook into the tile reveal handler" describes the prototype's client-SIMULATED reveal — no equivalent synchronous handler exists in our build.** Our tap only submits the move (fire-and-forget, `void`); the tile only genuinely opens once the server's broadcast updates `gameState` on a LATER render. Firing in the click handler would mean playing the sound even if the move never resolves as expected. Correct hook: detect the SERVER-CONFIRMED transition via an effect diffing `me.uncovered`/`me.bustedOn` against a "previously seen" snapshot — this also automatically, correctly excludes the `autoSafe` bulk reveal (never added to `me.uncovered` at all) with no extra condition needed.
- **A genuine design subtlety the prototype's own simpler model doesn't have to solve: must be scoped per-ROUND, not just per-match.** Confirmed via `mines.ts`: Mines has its own internal draw-replay mechanic (`round`, bumped on a tie, same match) that resets `me.uncovered` mid-match. A "seen" baseline that only resets on `currentMatchId` change (this file's own existing pattern for `resultPhase`) would silently skip the gem sound for a repeated tile index across a replay — needs keying off round too.
- **One correction to the ticket's own file path:** `apps/web/src/assets/sounds/`, matching our existing convention (`play.mp3`, `dice-roll.mp3`, etc.), not `assets/audio/` (the prototype's own path). Straightforward otherwise — copy both Designer-provided files in as-is, 2 `MANIFEST` entries.

**PM's implementation, independently verified against the diff — matches exactly, and the round-scoped baseline logic is precisely right.** `roundKey = ${currentMatchId ?? ''}:${view?.round ?? 0}`, correctly re-established (no retroactive fire) on any key change — new match, new round, or a fresh mid-round mount/reconnect. `currentMatchId` correctly threaded down through `MinesPanel` (Mines-only extension to `GameAreaArgs`, every other game's `renderGameArea` untouched). New 5-test suite covers exactly the right scenarios: incremental genuine reveal, genuine bust, `autoSafe`-at-lock exclusion (correct by construction — the effect diffs `me.uncovered` directly, never touched by the ghosted bulk reveal), no-retroactive-fire-on-reconnect, and the round-replay baseline reset (proving BOTH that the round transition itself never fires a stray sound AND that a repeated tile index correctly sounds again in the new round, not silently skipped as "already seen"). PM also reported verifying the 4 "should fire" tests genuinely catch a regression via a temporary-disable-confirm-restore pass — good empirical practice. Ran it myself: `MinesHub.test.tsx` 36/36.

**Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-22#1 — shipped in PR #679, independently re-verified against the diff and re-tested (DiceHub 30/30), deployed live at `rapidclash-00127-l4z`. Designer's D38: Dice's roll sometimes starts mid-track instead of at 0. Full detail: `ADVISOR_TO_PM.md` 2026-09-22#1.**

- **Neither of the prototype's own 2 cited reasons literally applies to our code — checked, not assumed.** Our `revealedRoll()` is already exactly `target × ease(p)`, no interpolate-from-current bug. Our reveal timing is real-clock-driven off actual server data, not the prototype's own scripted nested-`setTimeout` double-reset — there's no analogous mechanism to be missing.
- **The real, empirically-confirmed mechanism: a race between two independently-timed clocks, introduced by my own D35 change.** The reveal clock starts the instant the server delivers a result (`armedSig`), completely decoupled from `cubeActive`'s own 660ms artificial delay (added in D35 to fix the unrelated "cube pops in mid bar-split" bug). Before D35, the two were always synchronized for free (the old component only mounted, with the cube already active, once `live` became true — no gap existed to race). D35 introduced a real gap between "cube could be shown" and "cube is actually shown," and nothing re-synced the reveal clock to it. **A regression traced to my own D35 ticket, flagged plainly.**
- **Empirically reproduced BOTH the bug and its "sometimes" nature, with exact numbers.** Fast round-trip (server result already present when `live` flips true — realistic vs. bots): at the moment the cube becomes visible, the label already reads "46.50", not "0.00". Slow round-trip (result arrives ~600ms later): same moment, correctly "0.00". Confirms precisely why it's "sometimes" — depends entirely on how fast the round-trip happens to land relative to the fixed 660ms window.
- **Fix, verified in a scratch copy (not committed):** gate the reveal clock's own start on `active` too, not just `armedSig` — a 2-line change (`if (!armedSig || !active) {...}`, dependency array `[armedSig, active]`). Guarantees the clock always starts at whichever comes later. Applied it temporarily, re-ran both repro scenarios (fixed), then re-ran the FULL existing `DiceHub.test.tsx` suite: 29/29 unaffected (every existing test mounts already-live, so none exercise this race) — reverted before writing this ticket, PM's own implementation is what ships. Bonus: this also reproduces the prototype's own cited 460ms gap between cube-visible and count-up-starts, for free.
- **A second, related, lower-priority finding — flagged precisely, not silently folded in.** Designer's own check also describes fills easing from the last round's value during a search replay — confirmed our build doesn't do this (always shows 50%, the true-cold-idle value, every search). Confirmed this is pre-existing, NOT a D35 regression (the old, separate `DiceIdle` component had the identical hardcoded gap before D35 ever existed). Not recommending bundling into the same fix — architecturally more involved (needs new persisted state + a timing design call the prototype's own scripted reset doesn't map onto cleanly), and much less visible (board dimmed to 0.28 opacity during search). Recommending a separate follow-up ticket instead.

**PM's implementation, independently verified against the diff — matches exactly, character for character.** The 2-line fix landed verbatim (guard + dependency array on `active`). New test reproduces the exact fast-round-trip scenario empirically (result already known when the match goes live, well before the 660ms cube delay elapses) and proves BOTH that the count-up now correctly starts fresh at 0 once the cube becomes visible AND that it still lands on the real, correct value once the count actually runs — a proper before/after proof, not just a snapshot. Ran it myself: `DiceHub.test.tsx` 30/30 (up from 29, the new test accounted for). Not yet deployed — still on `rapidclash-00126-g2f`, merged but pending the next deploy.

D01-D35 plus every 2026-09-21/2026-09-22 ticket through D38 are now shipped and merged — this one's deploy is still pending. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket; the secondary finding (search-phase fill value) remains a separate, un-ticketed follow-up per Owner/Designer's own call whenever they want it.

---

**2026-09-21#11 — shipped in PR #676, deployed live at `rapidclash-00126-g2f`. Active incident (not historical): the missing-tiles problem recurred, worse than before, from a different cause. Full detail: `ADVISOR_TO_PM.md` 2026-09-21#11.**

- **Confirmed live, not just from logs.** Made 5 direct `curl` requests to a game-tile image myself: 5/5 got HTTP 429 ("no available instance"), right now. `gcloud logging read`: 244 of 922 requests (26.5%) rejected over 30 minutes — worse than D36's original 13.7% — despite `containerConcurrency` confirmed still correctly 300 on the live-serving revision itself, and no crash/restart events in that window.
- **The real mechanism this time: files that should never be re-fetched are being re-fetched 13-24 times each within 5 minutes, from one visitor.** Vite's own content-hashed build assets (`keno-B2ChlCB2.webp` etc. — by construction, never change for the same content) should be fetched from the network exactly once, ever, then served from the browser's own cache forever after.
- **Root cause: the static-file serving has no cache directives at all.** `server.ts:96`, plain `app.register(FastifyStatic, { root: webDist })` — no `maxAge`/`immutable`/`cacheControl`. Confirmed against the actual installed `@fastify/send@2.1.0` source: with no `maxAge`, the default is effectively `max-age=0` — technically "cacheable" but requires revalidating with the origin on every single use. Every reload of a frustrated visitor seeing broken tiles re-fetches everything from the ONE concurrency-capped instance instead of the local cache — actively undermining D36's own fix, a self-reinforcing loop.
- **Fix, precisely scoped — one real correctness trap identified and avoided.** Only `/assets/*` (confirmed the ONLY hashed directory via `ls apps/web/dist/`) should get a long-lived immutable cache. Everything else at the dist root (`index.html`, `sw.js`, `workbox-*.js`, `manifest.webmanifest`, `icons/`) must keep today's always-revalidate behavior — caching `index.html` or the service worker long-term would silently break every future deploy's rollout (visitors stuck on old code/a stale service worker). Use `@fastify/static`'s own `setHeaders` callback (confirmed supported by the installed version) to apply the long-lived header only under `/assets/`, leaving everything else untouched — one registration, one small conditional function.
- **Scope, per Owner's own explicit call:** Owner declined raising `containerConcurrency` further as part of this ("not a solution... just testers, not real users") — this ticket is ONLY the cache-header fix, not reopening the concurrency question.

**PM's implementation, independently verified against the diff, live headers, and tests — matches exactly, plus one worthwhile Cloud-Run-platform nuance PM flagged and I independently re-confirmed.** `setHeaders` correctly checks `path.includes(sep + 'assets' + sep)` (cross-platform-safe path separator) — no second `FastifyStatic` registration, one conditional. Re-ran a fresh `curl -I` pass myself (not just trusting the report): `/assets/*` → `Cache-Control: public, max-age=31536000, immutable`; `index.html`, `/`, `sw.js` → `Cache-Control: private, max-age=0` (correctly untouched, the safety boundary held). One nuance PM caught and I re-confirmed live: Cloud Run's own edge (`server: Google Frontend`) silently rewrites `public` to `private` for `/assets/*` specifically — a known GFE behavior (avoids accidental shared/CDN-cache pollution on app responses), not a bug in the fix. Doesn't undermine the actual goal: `private, max-age=31536000, immutable` still fully instructs the REQUESTING BROWSER's own local cache to hold the file for a year without revalidating — exactly what was needed, since there's no CDN in front of this service anyway. New `static-cache.test.ts` added; ran the full server suite myself: 225/225 (up from 220, the 5 new tests accounted for).

D01-D35 plus every 2026-09-21 ticket (#1-#11) are now all shipped, merged, and deployed live at `rapidclash-00126-g2f`. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-21#10 — shipped in PR #673, independently re-verified against the diff and re-tested. Designer's D37: Mines' opponent "Playing…" label is still showing when their gem strip fades in at reveal — the two collide in the same bar for the first second. Full detail: `ADVISOR_TO_PM.md` 2026-09-21#10.**

- **Confirmed exactly as reported** (the D37 screenshot shows both "Playing…" and a full 10-gem strip in the same opponent bar simultaneously) — but the prototype's own stated cause ("tied to your phase") doesn't literally describe our code. Our label is gated on the SHARED, whole-match `phase`, which already, correctly, reflects BOTH players (not just "me") — that part isn't broken.
- **The real gap: `phase` stays `'in-match'` through the whole post-lock hold window, with no awareness of Mines' own finer-grained reveal sequence.** `MinesHub.tsx`'s `resultPhase` state machine already, correctly, gates its `reveal` transition on `opp?.locked` (a real, already-shipped earlier fix) — the gem strip only ever appears once the opponent is genuinely done. But the shared `phase` doesn't reflect that sequencing at all, so "Playing…" — knowing nothing about `resultPhase` — keeps showing well after the strip has already faded in.
- **Worked the ticket's own two timing scenarios against our actual constants and they match exactly, with the fade invariant already satisfied for free.** Opponent-finishes-after-you: `reveal` already fires 820ms after `opp?.locked` flips true — 560ms more than the 260ms fade needs. Opponent-already-finished: converge/reveal/final land at +1500/+2320/+3020, byte-identical to the ticket's own numbers, with the label (once fixed) already faded out well before your own bust. **No reveal-timing changes needed — only the label's own visibility condition.**
- **A second, pre-existing gap needs fixing for this to actually work: no real fade exists today.** "Playing…" is a plain conditional-mount (`phase === 'in-match' && <span>...</span>`) — pops in/out instantly, the prototype's own cited `transition:opacity 260ms ease` was never implemented. Shared across every game, not Mines-specific.
- **Fix:** add an opt-in `oppLocked?: boolean` prop (same idiom as `oppGemRow`/`resultConverge` — a no-op for every game that doesn't pass it), Mines passes `opp?.locked ?? false`; change the render to an always-mounted, opacity-toggled span (`opacity: phase === 'in-match' && !oppLocked ? 1 : 0, transition: 'opacity 260ms ease'`) — now genuinely fading for every game, matching the prototype's own citation for the first time.
- **One layout side effect flagged, not solved unilaterally:** always-mounted-at-opacity-0 reserves the label's box space even while invisible, slightly narrowing the gem row's own `flex-1` room versus a true unmount — likely visually negligible given how compact both elements are, but worth a live-page look once shipped rather than assuming.
- **Checked RPS and Dice rather than assuming the same fix applies — verified, not guessed.** RPS deliberately never wires `holdResultMs` (confirmed via its own existing code comment) — both results land simultaneously, this bug's precondition doesn't exist there. Dice DOES use `holdResultMs` the same way Mines does, but its opponent bar has no competing gem-strip-equivalent element to collide with — no visual collision to fix, just a much smaller, optional wording-accuracy question (not folded into this ticket).

**PM's implementation, independently verified against the diff — matches exactly.** New `oppLocked?: boolean` prop threaded through `GameHubProps`/`OpponentSlot`/the render call site verbatim, `MinesHub.tsx` passes `opp?.locked ?? false`. The label render correctly switched from the conditional mount to an always-mounted, opacity-toggled span with the real `transition: 'opacity 260ms ease'`. New test (`MinesHub.test.tsx`) proves the exact mechanism precisely: label visible while I've busted but opponent hasn't locked, then hides IMMEDIATELY (same render, no timer advance) the instant the opponent locks — well before `reveal` fires 820ms later. Ran it myself: MinesHub 31/31. Also ran RPS/Roulette/Blackjack/Dice's own suites (all share this label) to check for regressions from the always-mounted change: 119/119, clean.

D37 is now shipped, merged, and confirmed correct — deploy status not yet re-checked (batched with 2026-09-21#11 once that lands). **Advisor next:** available, no open thread on this ticket. **PM next:** nothing pending on this ticket.

---

**2026-09-21#9 — shipped in PR #670, deployed live at `rapidclash-00125-cnn`, `containerConcurrency` confirmed 300 via `gcloud run services describe`. Owner's own direct report (D36): testers see half-loaded tiles and broken navigation, several days running. Full detail: `ADVISOR_TO_PM.md` 2026-09-21#9.**

- **Confirmed with hard data, not just the dashboard Owner sent.** `gcloud logging read` on the last 24h: **1,129 of 8,213 requests (13.7%) got a literal HTTP 429** — images, a font, and plain API routes all rejected. This is Cloud Run's OWN infrastructure-level rejection, distinct from the app's `@fastify/rate-limit` (which only ever applies to `/auth/guest`/`/rewards/claim`). Still happening in the current hour when checked.
- **Root cause: the single mandatory instance's concurrency ceiling (80) was never deliberately set — it's Cloud Run's bare default.** `docs/DEPLOY.md`'s own canonical deploy command never passes `--concurrency`. `--max-instances 1` itself is correct and deliberate (ADR-009, in-memory state + local SQLite) — not what needs to change.
- **What's consuming the budget: the always-on bot-crowd holds 32 WebSocket connections continuously** — SSH'd into the demo-taker VM and confirmed its actual roster via `TAKER_ONLY_GAMES`, matching `tools/bot-crowd/README.md`'s documented formula (4 games × 4 bots + 8 games × 2 bots = 32). That's ~40% of the 80-slot budget before any real visitor arrives.
- **Precisely WHEN it happens, from the raw Cloud Monitoring metric (queried via the REST API, not eyeballed) — a deploy-time reconnect burst, not sustained all-day overload.** Real concurrency peaked at ~50 during calm periods (never reached 80). But 759 of the 1,129 total 429s landed in the exact hour `rapidclash-00123-rt9` (today's D30-D35 batch) deployed; 297 more clustered around another deploy-adjacent hour. When the bot-crowd's 32 connections all reconnect within seconds of a restart, it briefly spikes past 80. Deploys have happened nearly every ticket cycle this week, each followed by a bot-crowd restart — explaining "several days" precisely.
- **A compounding gap found in the code: no WebSocket heartbeat.** `gateway.ts`'s `close` handler does correct, thorough cleanup, but only fires on an actual close — nothing detects a connection that died WITHOUT one (common on mobile: lost signal, backgrounding). A dead connection can occupy a slot for up to Cloud Run's own 3600s hard cap.

**Two items, both Owner-approved (final number: 300):**
1. **Raise `containerConcurrency` from 80 to 300** — a Cloud Run service-config change, not code (`gcloud run services update --concurrency 300`); doesn't touch `--max-instances` or the single-writer architecture. Also add `--concurrency 300` to `DEPLOY.md`'s canonical deploy command so a future full redeploy doesn't silently revert to the 80 default.
2. **A WebSocket heartbeat** — low effort, reuses the existing `sweepTimer` periodic-check idiom and the existing `close`-handler cleanup path (via `socket.terminate()`) already in `gateway.ts`. Owner's own read is right that item 1 alone likely covers the reported case — but this closes the underlying gap for comparatively little code, so doing both.

**PM's implementation, independently verified against the diff, live config, and real production traffic — matches exactly, and is genuinely careful.** `containerConcurrency` confirmed `300` via `gcloud run services describe` (not just trusting the report), `maxScale` confirmed untouched at `1`. Heartbeat: `socketAlive` is a `WeakMap<WsSocket, boolean>` (auto-GC'd, no manual cleanup needed), `heartbeatIntervalMs` is correctly FUNCTION-scoped (read inside `registerWsGateway`, not a module-level `const`) — the exact pitfall that would have made an env-var override silently inert in tests, same shape as the existing `forfeitDelayMs`. Sequencing is sound: a socket survives one full missed interval before termination (never killed on its very first un-ponged tick), and a fresh connection starts marked alive so it's never prematurely terminated before its first ping. `terminate()` correctly reuses the EXISTING `close` handler verbatim — no new cleanup path. New `heartbeat.gateway.test.ts` (2 tests) proves both directions: a healthy connection survives many ticks, one with auto-pong disabled gets terminated within two ticks via the real close handler. Ran it myself: 2/2. Full server suite: 220/220, matching PM's reported count exactly.

**Real-world confirmation, not just tests — checked actual production traffic since the new revision deployed.** Queried Cloud Logging for everything since `rapidclash-00125-cnn`'s exact deploy timestamp (19:28:10Z): **zero HTTP 429s out of 116 requests** — this window includes the bot-crowd's own post-deploy reconnect burst (all 32 bots), the exact worst-case scenario this ticket was written to fix. Previously, an equivalent deploy-time window accounted for the large majority of the 24h's 1,129 429s.

**One credited, non-blocking finding from PM worth recording:** `SWEEP_INTERVAL_MS` (the EXISTING matchmaking-sweep interval, unrelated to this ticket) is a genuine module-level `const`, frozen at import time — `disconnect-forfeit.gateway.test.ts`'s own `CHALLENGE_SWEEP_MS='15'` override (set in `beforeEach`) never actually takes effect, since the module already evaluated it once before any test ran. Confirmed this independently by reading both files directly — it's real, but benign: the affected tests still pass (their own `await`/timeout margins are generous enough to tolerate the real, un-overridden 1000ms interval), so nothing is broken today, just less precisely-timed than its own comment claims. Not turning into a ticket — noting it here for whoever next touches sweep timing.

D01-D35 plus every 2026-09-21 ticket (#1-#9) are now all shipped, merged, and deployed live at `rapidclash-00125-cnn`. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-21#8 — shipped in PR #667, deployed live at `rapidclash-00124-jcs`. Owner's own direct report: the production site occasionally shows "Rate exceeded" on open — not a load/capacity issue, a specific rate-limiter bug. Full detail: `ADVISOR_TO_PM.md` 2026-09-21#8.**

- **Owner asked for help finding Cloud Run's monitoring console (to check "VM load") — pointed them there, but traced the actual error to its source rather than stopping at the dashboard.** `POST /auth/guest` is rate-limited to 5/minute, keyed by `request.ip` (`@fastify/rate-limit`'s default) — the code's own comment confirms the intent is per-visitor ("a real visitor... stays well under this; a scripted hammer... does not").
- **The real bug: `trustProxy` is never enabled, so `request.ip` is useless on Cloud Run.** `Fastify({ logger: false })` (`server.ts:135`) passes no `trustProxy` option (confirmed via repo-wide grep — not set anywhere). Cloud Run's container always sees Google's Front End as its TCP peer, never the real visitor; the real client IP only exists in `X-Forwarded-For`, which Fastify ignores without `trustProxy`. Every request today, from every visitor, resolves to the SAME `request.ip` — the "5/minute/IP" bucket is actually "5/minute, period," shared site-wide, bot-crowd included.
- **Explains "occasionally" precisely** — it only trips when enough guest-logins (real or bot) land in the same 60-second window to burn through 5 for the WHOLE SITE, which gets worse the more the demo is actually being used.
- **Checked for side effects first:** `request.ip` is read in exactly one place in the whole codebase (this rate-limit config) — no other route or abuse-detection logic depends on it, so the fix has no other blast radius.
- **Fix:** add `trustProxy: true` to the `Fastify(...)` constructor (`server.ts:135`) — the standard setting for any Fastify app behind one well-known reverse proxy (Cloud Run's GFE, an ALB, nginx). Restores the limiter to its documented original intent (a best-effort per-visitor anti-abuse limit, not a hardened security boundary — same category as before, just actually working).

**PM's implementation, independently verified against the diff — matches exactly, and confirmed directly against the installed package, not just general knowledge.** `trustProxy: true` added to the `Fastify(...)` constructor verbatim, with a doc comment matching the ticket's own reasoning. Checked `@fastify/rate-limit`'s actual installed source (`node_modules/.pnpm/@fastify+rate-limit@8.1.1`) directly — its default `keyGenerator` is literally `(req) => req.ip`, confirming the ticket's core factual claim beyond just citing the plugin's docs from memory. New test proves the fix empirically, not just structurally: uses `app.inject`'s `remoteAddress` to simulate the one shared proxy peer, with distinct `X-Forwarded-For` headers simulating distinct real visitors — burns through one visitor's 5/minute cap, confirms they're blocked, then confirms a SECOND visitor behind the same simulated proxy is completely unaffected (exactly the bug this fixes). Ran it myself: 4/4. Ran the full server suite: 218/218. Also did a direct smoke-test `POST` against the live deployed endpoint myself (not just trusting PM's report): `201`, confirming normal guest login still works in production.

D01-D35 plus every 2026-09-21 ticket (#1-#8) are now all shipped, merged, and deployed live at `rapidclash-00124-jcs`. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-21#7 — NOT A BUG, investigation closed. Owner/Designer report: the DemoGM account saw 3 natural blackjacks in a row, then a run of hands totaling 13. Full detail: `ADVISOR_TO_PM.md` 2026-09-21#7.**

- **Traced the entire RNG chain and found no weak link.** Match seeds come from `crypto.randomBytes(4)` (genuinely high-entropy, fresh per match, both match-creation paths) — not wall-clock time or anything predictable. That seeds a deterministic `mulberry32` RNG for the match's lifetime (a deliberate replay/fairness property, not a shortcut). Blackjack's own per-round shuffle derives from a second `rng.int()`-drawn base seed, mixed per `(round, playerIndex)` via a multiply+xorshift hash, then Fisher–Yates.
- **Empirically tested the actual shuffle code (2M+ trials), not just read it.** Natural-blackjack rate: 4.846% measured vs. 4.827% true combinatorial rate — no bias. `mixSeed`'s avalanche: consecutive rounds differ by an average of 15.97/32 bits (textbook-good). Player 0 vs. player 1 decks: zero collisions in 100,000 trials. Both-players-simultaneous-BJ rate: 0.2323% measured vs. 0.2330% expected under independence — no hidden correlation.
- **The reported pattern is ordinary variance, not an anomaly.** A match only continues past one round on a draw, so "3 BJs in a row" almost certainly means 3 separate matches, each with an independent crypto-random seed — probability ≈1-in-8,900 for one sitting, unremarkable across the demo's actual multi-day bot-crowd hand volume. Simulated 200,000 matches walking 15 rounds each: a same-player 3-in-a-row streak at a specific total (13) occurred in 1.07% of them — genuinely-random shuffles produce short streaks like this routinely, not rarely.
- **Ruled out the recent D30-D35 deploy as a cause** — `git log` shows no commits to Blackjack's dealing code, the shared RNG, or match-seed sourcing anywhere near today's Dice/Mines-only changes.

**Advisor next:** available, no open thread — no code change recommended. **PM next:** nothing pending; flag if Owner/Designer wants a live-page spot-check on DemoGM's actual match history instead of/alongside this code-level analysis.

---

**2026-09-21#6 — shipped in PR #664 (confirmed independently via `gh pr view` + `git show` against the diff), deployed live at `rapidclash-00123-rt9`. Designer's D35: Dice's Play→roll motion doesn't match — cubes snap in during the bar-split instead of after it, and replaying after a result wipes the board instantly instead of fading it. Full detail: `ADVISOR_TO_PM.md` 2026-09-21#6.**

- **Root cause is deeper than a missing transition string — confirmed by tracing exact render-by-render phase transitions, not just checking CSS.** `DicePanel` renders either `<DiceIdle>` or `<DiceBoard>` — two ENTIRELY SEPARATE components — based on `live = phase === 'in-match' || phase === 'result'`. No CSS transition can bridge an unmount/mount regardless of what's declared on either side. Every other element in this screen (bars, ring, VS, card... except Dice's own board) is a single persistent element whose values merely change, so their transitions already work correctly.
- **Symptom 1 (cubes appear mid-split) — a real timing gap, not just a missing fade.** Our `barSlideActive` is a single boolean (no "found"/"split" sub-stage like the prototype's own `rpsMatch` state machine) — bars slide back the INSTANT it goes false, which is the EXACT SAME render `holdSearch` clears and `phase` flips to `'in-match'`, mounting `DiceBoard` with its cube already fully visible. The prototype gets its own ~660ms gap for free from a genuinely later, separate `setState` (`diceCubeIn:true`, `:3421`) than the one ending the shift (`:3413`) — our simpler mechanism has no equivalent delay.
- **Symptom 2 (replay wipes instantly) — same mechanism, reverse direction.** Leaving `'in-match'`/`'result'` back into `'waiting'` (a new search) flips `live` true→false, unmounting `DiceBoard`'s entire resolved subtree (cubes, ring, fills) in one render instead of it fading/overlapping with the card's own dim-in.
- **A secondary effect of the same swap, not separately reported:** the card's own opacity fade-in at `run` is also currently a snap for the identical reason — fixed for free by the same structural change.
- **One minor, NOT Dice-specific finding, flagged but not folded in:** the shared win/lose/draw ring's box-shadow transition (`GameHub.tsx`'s `OwnSlot`/`OpponentSlot`) runs at 300ms (Tailwind's `duration-300`) vs. the prototype's own cited 380ms for that shared bar block (`Full Spec.html:437`/`:661`) — pre-existing, affects every game equally, not part of Designer's own two reported symptoms.
- **Fix:** merge `DiceIdle`/`DiceBoard` into one always-mounted component (matching the prototype's own persistent single-div architecture) so every existing transition string starts working across every phase boundary — fixes symptom 2 and the card fade-in for free. Symptom 1 additionally needs a new Dice-specific timing gate (a `setTimeout` off the same `holdSearch`→false edge, sized to the bar's own 620ms transition) since nothing today provides that gap.
- **Coordination note:** this unification would make `2026-09-21#5` (D34, history belt missing from idle) moot on its own if it lands first — neither blocks the other, but worth checking for redundancy once one ships.

**PM's implementation, independently verified against the diff — matches exactly, and it's genuinely careful.** `DiceIdle`/`DiceBoard` merged into one `DicePanel`, matching the prototype's persistent single-div architecture. `cubeActive`/`wasLive` correctly seeds from `live` itself (not hardcoded `false`) so a fresh mount already live (reload/reconnect mid-match) shows the cube immediately, with the ~660ms `CUBE_REVEAL_DELAY_MS` timer only arming on a genuine within-lifetime `false→true` edge — exactly the distinction the ticket needed. PM also documented (not silently made) a deliberate simplification: not splitting the fill's ~40ms-earlier reset from the cube's own delay, since the gap is cosmetically negligible relative to the reported symptom. As correctly predicted, this superseded D34's own separate `DiceIdle` threading (`2026-09-21#5`) rather than duplicating it — confirmed via `git show`, the merged component only renders the belt from one place now. Tests are excellent: one proves the ~660ms delay empirically via `rerender` (idle→live, mid-transition), another proves the structural claim directly — `hub-board`/`dice-cube-mine` assert `toBe()` the SAME DOM node across a replay/leave transition, not just "still renders something." Ran `DiceHub.test.tsx` myself: 29/29 clean.

D30-D35 are now all shipped, merged, and deployed live at `rapidclash-00123-rt9`. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-21#5 — shipped in PR #663, superseded by `2026-09-21#6`'s (D35) unification rather than duplicated. Designer's D34: the Dice history belt vanishes entirely during matchmaking instead of dimming with the rest of the card. Full detail: `ADVISOR_TO_PM.md` 2026-09-21#5.**

- **Confirmed exactly as reported; ruled out all 4 of Designer's own named suspects first.** Nothing clears `history` on Play, in the search sequence, or on `startDice`'s own reset — Designer's own trace of the data lifecycle (written once at a landed result, cleared only on leaving the screen) is exactly right.
- **The actual mechanism: two separate component trees, and the belt only exists in one.** `DicePanel` swaps entirely between `<DiceIdle>` and `<DiceBoard>` based on `live = phase === 'in-match' || phase === 'result'` — false for the ENTIRE matchmaking window. `<DiceHistoryBelt>` is only ever rendered from inside `<DiceBoard>`; `DiceIdle`'s own JSX never renders it at all. Not a state bug — the component holding the belt simply isn't mounted during the search.
- **The prototype never has this seam because it never splits these into two components** — its own `isDice` block is ONE persistent wrapper div, dimmed as a whole, with the belt as a direct child alongside the tracks. Traces to the idle-preview split itself (built as a resting-tracks "anchor" preview, per this file's own header comment) never having had the belt added to its scope — not a specific later regression.
- **Fix:** add `<DiceHistoryBelt>` to `DiceIdle`'s own render, threading `history` down from `DicePanel` (already available there, just not forwarded) — placed as a direct child of the outer 266px box (not nested in the existing tracks wrapper), matching exactly how `DiceBoard` already lays it out so its `mt-auto` pins it to the bottom edge. No changes needed to the belt itself, the opacity dimming (already correctly shared), or `history`'s own lifecycle.

**PM's implementation, independently verified against the diff — matches exactly.** `DiceIdle` gained `history` as a prop and rendered `<DiceHistoryBelt>` as a sibling of the tracks wrapper, verbatim as scoped. Shipped in the same PR (#663) as D33. Superseded (not duplicated) by `2026-09-21#6`'s (D35) later component-merge, which only has one branch to render the belt from now — confirmed no redundant rendering remains.

**Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-21#4 — shipped in PR #663, deployed live at `rapidclash-00123-rt9`. Designer's D33: the Dice lobby's two idle fills sit at a near-empty 10px nub instead of the prototype's neutral 50%. Full detail: `ADVISOR_TO_PM.md` 2026-09-21#4.**

- **Confirmed real, and it's my own earlier ticket's citation error, not a fresh bug.** `DiceTrack`'s current comment (written for `2026-09-16#4` item 3) claims the prototype's own position and fill formulas "fall back to the SAME value (0)" at rest — checked directly against the prototype's source, and this isn't true. `diceMyPos` (`:3608`) uses `|| 0` (null and 0 both resolve to the same left-edge position). `diceMyFill` (`:3676`) uses an explicit `== null ? '50%' : ...` — null and 0 genuinely diverge there: null (true lobby idle, never rolled) shows a neutral half-filled bar; 0 (a real value the prototype explicitly resets to the instant a match search begins) shows the same near-empty nub the position formula also rests at.
- **`2026-09-16#4` item 3 correctly fixed the wrong-diverging case, and over-generalized into the right-diverging one.** That ticket's own concern was `DiceBoard`'s mid-round wait (cube visible, roll not yet known) — correctly unifying cube position and fill there, matching the prototype's own `diceMy = 0` state. But its `value = roll != null ? ... : 0` fallback applies unconditionally, including from `DiceIdle` (true lobby idle) — the one case that should have kept the prototype's OTHER resting value (50%) instead.
- **Fix, precisely scoped:** `DiceTrack` already receives an `active` prop that is `false` only from `DiceIdle` and `true` only from `DiceBoard` — exactly the structural distinction needed. Give `fillWidth` its own fallback gated on `active`: `50%` when `!active`, the current empty-nub formula when `active` (unchanged). `cubeLeft` needs no change — the prototype's own position formula never distinguished null from 0 either, and the cube is invisible (`opacity:0`) in the only state this affects.

**PM's implementation, independently verified against the diff — matches exactly.** `fillWidth` gated on `active` separately from `cubeLeft`, `cubeLeft` left untouched, verbatim as scoped.

**Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-21#3 — shipped in PR #661, deployed live at `rapidclash-00123-rt9`. Designer's D32: Dice cubes render fully visible but textually blank at round start, and drift visibly out of sync with their own number label during the roll. Full detail: `ADVISOR_TO_PM.md` 2026-09-21#3.**

- **Item 1 (blank cubes) — the label's null-fallback disagrees with the cube's own position fallback.** The cube (SVG, opacity, scale) is already correctly visible from the instant a match goes live, matching the prototype's own `diceCubeIn` gate. But the number label inside it falls back to `''` whenever `roll` is `null` — which it stays for the entire indeterminate server round-trip plus the first 460ms of the reveal clock. The prototype never has this gap because it seeds `diceMy`/`diceOpp` to `0` (not null) in the SAME state update that makes the cube visible, so its own label reads "0.00" from frame one. Fix: match the label's fallback to the position/fill's own already-established `0` fallback — "0.00" while the cube is active, blank only when it isn't shown at all.
- **Item 2 (number/cube desync during the roll) — a leftover CSS transition fighting a per-frame value.** `roll` already updates every animation frame during the 444ms count-up (the reveal clock's own per-frame `tick()`), and the number label renders that instantly with no CSS involved. But `cubeLeft` and `fillWidth` are BOTH wrapped in an unconditional CSS transition that never gates off during that per-frame drive — so the browser is perpetually smoothing toward a target that keeps moving, and the cube/fill visibly lag behind the number, most noticeably right as the number lands on its true final value. The prototype never fights itself this way: there's no position transition in its source at all for the live roll, and its own fill transition is explicitly `'none'` while a roll is in progress. Fix: gate `left`/`width`'s transitions off during the active per-frame count, mirroring the prototype's own `diceRolling` gate exactly.
- **Both fixes are local to `DiceHub.tsx`, no protocol/server changes, independent of each other and of `2026-09-21#2`.** Item 2 traces back to my own earlier `2026-09-12#5` ticket's transition, written before `2026-09-16#4` (D21) added the per-frame reveal counter on top of it without regating it — same "two features individually correct, wrong once combined" shape as `2026-09-21#1`'s own root cause.

**Advisor next:** available, no open thread. **PM next:** dispatch — both fixes are small and local to one file, safe to ship together.

---

**2026-09-21#2 — shipped in PR #656, deployed live at `rapidclash-00123-rt9` (confirmed independently via `gh pr view` + `git show` against the diff, and `gcloud run services describe`). Designer's D31: the opponent's gem strip and caption always show 0 at reveal, despite a correct win/lose verdict. Full detail: `ADVISOR_TO_PM.md` 2026-09-21#2.**

- **Ruled out the client first, by feeding it real data directly.** Rendered the real component with the opponent's `score` already populated (both the "already done" and "hold-and-release" cases D31 describes) — both correctly displayed the real count. The client's own binding is already correct.
- **The real bug: a genuine server gap, 100% reproducible, not intermittent.** `viewFor`'s terminal branch (`packages/games/mines/src/mines.ts`) returns an entirely different, raw shape (`{...s, mines}`) instead of the carefully-constructed `oppView` the non-terminal branch builds — meaning `score` is never constructed at all once terminal. Confirmed `terminal(s)` fires on the EXACT SAME condition (`both locked`) that would populate `score` in the first place — so by the time the client's own `resultPhase` reaches `reveal`, the server has already switched to the branch that never builds it. Explains the asymmetry too: the player's own count reads `uncovered.length` (a field that survives both branches); the opponent's reads `score` (constructed only in one).
- **Fix belongs server-side:** the terminal branch needs to also include a `score` field for both players, computed the same way the non-terminal branch already does.
- **A deliberate wording correction from Designer, applying as directed:** drop the singular `"1 gem"` form from the player's own caption (a knowing, explicit override of the prototype's own literal citation this time, not a misreading) — both sides always `{n} gems`.
- **One framing correction, not a silent reinterpretation:** D31's own "same value the verdict reads" check doesn't map onto our architecture — our verdict is a separate, already-server-computed `Outcome`, never wired to any gem-count field at all. Noting this so nobody looks for a second source to reconcile against.

**PM's implementation, independently verified against the diff — matches exactly.** The terminal branch now builds `boards` via `Object.fromEntries(s.players.map((p) => [p, { ...s.boards[p], score: score(s.boards[p]) }]))` — the same `Object.fromEntries` idiom this file already uses for `forfeit`'s own boards rebuild, computing `score` for BOTH players (not just the opponent — a deliberately consistent shape, confirmed correct even though the client's own `myGemCount` never needed it). `ownGemText` dropped its singular branch verbatim, applying D31's override exactly as directed. New test in `mines.test.ts` asserts `score` is present on both boards from either player's own point of view at terminal — the precise shape that was missing. Re-ran the suites myself rather than trusting the reported counts: `mines.test.ts` 52/52, `MinesHub.test.tsx` 30/30, full web suite 909/909, full server suite 217/217 (rebuilt `packages/core` proactively first — the recurring stale-dist quirk in this worktree, not a regression).

D30 + D31 are now both shipped, merged, and deployed live at `rapidclash-00123-rt9`. **Advisor next:** available, no open thread. **PM next:** nothing pending on either ticket.

---

**2026-09-21#1 — shipped in PR #653, deployed live at `rapidclash-00123-rt9` (confirmed independently via `gh pr view` + `git show` against the diff, and `gcloud run services describe`). Designer's D30: the bars converge, then shift again when `final` lands, closing over the VS label. Full detail: `ADVISOR_TO_PM.md` 2026-09-21#1.**

- **Confirmed real; ruled out all 3 of Designer's own listed candidates by measurement, not reasoning.** Rendered the real component through the full converge→reveal→final sequence and read the bars' actual `transform` value out of the DOM at each phase — byte-identical across all three. Rules out re-measurement (candidate 1) and a second transform target (candidate 2) directly; also confirmed the win ring is box-shadow-based (no layout effect) and the fill/verdict text are both `position:absolute` (already out of flow), ruling out the literal form of candidate 3 too.
- **The actual mechanism: an ordinary flex-column reflow, not the bar's own styling.** The round clock sits inside the board, a flex SIBLING of both bars in the shared column layout (matching the prototype's own identical `display:flex; flex-direction:column` game-wrap structure). The clock collapses to 0 height on `phase === 'result'` — a state that, by design, lands right around `final`. Collapsing an earlier sibling's height pulls every later sibling upward through normal reflow — the own bar moves up toward the opponent bar, closing the gap and covering VS, without the bar's own transform ever changing.
- **Traced this to my own earlier `2026-09-16#5` ticket's own imprecise timing proxy, not a new regression, and correcting it plainly.** The prototype's own clock-collapse condition flips synchronously at the bust/timeout itself — 1500ms (or 500ms) BEFORE `converge` even starts — so any reflow it causes is fully settled long before the bars ever move. My own earlier ticket used `phase === 'result'` as "the cleanest local proxy available" at the time, checked in isolation before the bar-convergence feature existed — reasonable then, wrong once combined with the later-built sequence.
- **Fix: swap the proxy for the real signal, already in scope in the same function.** `MinesBoard` already computes `myLocked` for its own tile logic — the same signal the prototype's own gate tracks, firing at the exact right moment. Swap both of the round clock's conditions from `phase === 'result'` to `myLocked` — a two-token change, no new props.

**PM's implementation, independently verified against the diff — matches exactly, plus a small, sensible cleanup.** Both conditions swapped to `myLocked` verbatim; also dropped the now-unused `phase` parameter from `MinesBoard`'s own destructuring (no longer referenced anywhere in that function) — a correct bit of housekeeping, not scope creep. The test was meaningfully strengthened, not just updated: it now proves the clock collapses immediately at lock while STILL in-match (no result phase, no `currentMatchId` change at all) — precisely the earlier-timing behavior the fix establishes, not just "collapses eventually." Re-ran the suite myself: 30/30 green.

D30 is now shipped, merged, and deployed live at `rapidclash-00123-rt9` (batched with `2026-09-21#2` and every Dice ticket, see entries above). **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-18#2 — shipped in PR #650, deployed live at `rapidclash-00122-zkz` (confirmed independently via `gh pr view` + `git log` + `git show` against the diff, and `gcloud run services describe`). Owner/company-management feedback (not from Designer), four distinct findings from one report. Full detail: `ADVISOR_TO_PM.md` 2026-09-18#2.**

- **Item 1 ("no sound when playing") — not a bug, a real content gap, not building it this round.** The sound engine itself is correctly built (assets, mobile-unlock, mute); the actual finding is that only 3 UI actions in the whole app ever trigger a sound at all (shared PLAY click, Dice roll/win, Chess move) — Mines/RPS/Blackjack/Coinflip/etc. have zero gameplay sounds. Given recent testing has been almost entirely Mines, this fully explains the report. Flagged for the record; no code this round.
- **Item 2 ("JOIN occasionally does nothing") — confirmed real and deterministic, not intermittent.** The rejection notice (`CHALLENGE_TAKEN`/`SELF_TAKE`/`INSUFFICIENT_BALANCE`) is correctly computed but rendered only inside the legacy `StakeEntryScreen`, unreachable for any of the 12 currently-hub-routed games — confirmed via `HUB_GAMES`/`hubScreenFor()`. Fix: redirect it to the same always-visible, fixed-position banner `actionNotice` already correctly uses.
- **Item 3 (new reject-sound cue) — Owner-approved minimal scope after a real tension I raised directly.** "Make it imperceptible so it's low-risk" doesn't fully hold together — a cue either registers (a genuine sound-identity addition, worth Designer's input) or it doesn't (doesn't solve the reported problem). Owner's own resolution: rejection-only, framed as a functional bug fix, revisit/expand once Designer is back. Scoped to 3 already-recognized rejection moments (JOIN failure, PLAY-with-no-bet, Play-a-Friend's confirmed real no-op). There's already a clean, licensing-free precedent for generating the asset (`gen_move_wav.py`, self-authored/CC0).
- **Item 4 (bet amount missing after JOIN) — confirmed real asymmetry, precedented fix.** The bet-amount display is gated on purely local, client-side state a JOINing player never populates (they never touch the bet-selector UI). Fix mirrors an exact existing precedent: `MatchStartPayload` already solved the identical PLAY-vs-JOIN parity problem once for the opponent's name; add a `stake` field the same way.

**PM's implementation, independently verified against the diff — matches exactly as scoped, and the item 4 protocol change specifically got the trust boundary right.** Item 2: `challengeNotice` rendered via the same fixed `.ws-banner-error` treatment as `actionNotice`, kept as a genuinely separate element (not merged) with a clear reason why (different lifecycles — `actionNotice` persists until reconnect, `challengeNotice` clears on the next JOIN attempt). Item 4: `MatchStartPayload.stake`/`MatchStatePayload.stake?` added exactly mirroring `opponentName`'s own shape (required on start, optional on resume, omitted on per-move broadcasts); threaded through all 3 `deliverMatchStart` call sites — checked the take-challenge one specifically, since it's the one that matters most here: it correctly reads `match?.stake` (the server's own authoritative record), not any client-supplied value, exactly the trust boundary the ticket asked for. `GameHub.tsx` syncs `armedStake` from the new `matchStake` prop in a `currentMatchId`-keyed effect, matching the file's own established pattern (same shape as the `2026-09-18#1` scroll effect). Item 3: `reject.wav` generated via the same self-authored/CC0 synthesis technique as `move.wav` — confirmed the actual parameters produce a genuinely distinct, quiet cue (110Hz vs `move.wav`'s 180Hz, peak amplitude 0.22 vs 0.8, gentle decay vs a percussive knock) — wired to exactly the 3 rejection moments scoped, with no double-play risk on the two moments that share one code path (`guideToBet`).

**Independently re-ran the tests myself rather than trusting the count alone** — the web suite (81/81 across `App`/`RpsHub`/`MinesHub`) passed cleanly first try; the server suite initially showed 137 failures from a `createChatTransport is not a function` error, traced to a stale, week-old `packages/core/dist` build in my OWN worktree (not a regression — the export exists correctly in source) — rebuilt it and re-ran clean, 217/217, matching PM's own report exactly.

2026-09-18#2 is now fully shipped, merged, and deployed. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-18#1 — shipped in PR #647, deployed live at `rapidclash-00121-vfl` (confirmed independently via `gh pr view` + `git log` + `git show` against the diff, and `gcloud run services describe`; also re-ran the full test suite locally, 78/78 green). Owner/company-management feedback (not from Designer): JOINing a live challenge from a scrolled-down list leaves the viewport at its old position once the game screen swaps in, so the user can miss the match. Full detail: `ADVISOR_TO_PM.md` 2026-09-18#1.**

- **Confirmed real, root cause precise.** The app is a single page with no internal scroll container — the whole document scrolls normally. JOIN doesn't change what's rendered by itself; the actual screen swap happens later in `App.tsx`'s `onMatchStart` handler via `setScreen(hubScreenFor(payload.gameId) ?? 'play')`, an ordinary React state change with no browser navigation to trigger a scroll reset. Confirmed this affects PLAY identically (same code path) — just far less visible, since PLAY is usually pressed from higher up on a hub's own screen.
- **Fix, scoped narrowly per Owner's own explicit direction (not the broader "every navigation" version):** one new effect inside `GameHub.tsx` (the shared component every hub game renders through), keyed on `currentMatchId` transitioning to a new truthy value — `useEffect(() => { if (currentMatchId) window.scrollTo({top:0, behavior:'auto'}); }, [currentMatchId])`. Covers JOIN and PLAY identically since both flow through the same transition. There's already a precedent for exactly this shape of effect a few lines away in the same file.
- **The broader question was raised to Owner directly, not decided unilaterally** — extending this to every top-level navigation is a common, low-risk SPA pattern I'd lean toward, but it touches every screen and Designer hasn't weighed in. Owner's own call: ship the narrow fix now, take the general policy to Designer separately on their own timeline.

**PM's implementation, independently verified against the diff — matches exactly as scoped, one line, plus a well-targeted test.** The effect landed verbatim (`currentMatchId`-keyed, `behavior:'auto'`). PM's own test asserts all three cases precisely: idle mounts with no scroll call, a `currentMatchId` transition scrolls exactly once with the right args, and a re-render with the SAME `currentMatchId` doesn't re-fire. Also added a sensible global `window.scrollTo` stub to the shared jsdom test setup (jsdom doesn't implement it, and this real call now fires in most hub tests, not just this ticket's own) — a reasonable, well-explained infrastructure fix, not scope creep.

This ticket's own broader "every navigation" question remains deliberately unaddressed, per Owner's own explicit call — nothing pending there unless Owner brings it back.

2026-09-18#1 is now fully shipped, merged, and deployed. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-17#7 — shipped in PR #644 (bundled with D28), deployed live at `rapidclash-00120-xrb` (confirmed independently via `gh pr view` + `git log` + `git show` against the diff, and `gcloud run services describe`). Designer's D29: the board darkens momentarily on every tile tap, before the tile reveals. Full detail: `ADVISOR_TO_PM.md` 2026-09-17#7.**

- **Confirmed real; root cause is more precise than any single one of Designer's own 4 listed candidates, though closest to #4.** `MinesPanel`'s own board-wide opacity (`args.barSlideActive`) is genuinely untouched by a tap — ruling out a board-level flag, a re-mount, or a container-level press style. The real mechanism: `MinesBoard`'s own tile styling applies Tailwind's `opacity-70` to any `'covered'` tile that isn't currently `clickable`. The instant ANY tile is tapped, `App.tsx`'s SHARED `handleMakeMove` (used by every hub, not Mines-specific) calls `setLegalMoves([])` right after a successful submit — with `legalMoves` empty, every other still-covered tile's `clickable` check fails too, so all 24 of them pick up `opacity-70` for the entire round-trip window until the next WS broadcast lands. Fires on every tap, safe or mine, matching the report exactly. The tile-level opacity has no transition set (only background does) — it snaps instantly rather than fading, a sharper flicker than a smooth board-wide dim would produce.
- **Confirms the caveat at the top of D29 itself — no optimistic reveal needed.** The tapped tile's own reveal is already correctly, honestly gated on server confirmation (`cellKind()` never returns `'safe'` before `myUncovered` — server-confirmed — includes it). Nothing about that changes. The fix is exactly what the caveat predicts: stop the OTHER tiles from visually reacting to a state that has nothing to do with them.
- **Fix, narrowly scoped:** drop `opacity-70` from the covered-tile styling; keep `disabled={!clickable}` (the functional gate, correctly prevents a double-tap mid-round-trip) without pairing it with a visible dim. A `'covered'` tile only ever exists while actively, un-locked mid-round (confirmed via `cellKind()` — once locked, the fallback is `'autoSafe'`, never `'covered'`), so there's no other legitimate state this opacity was protecting.
- **Scope:** the shared `setLegalMoves([])` mechanism itself is correct and needed elsewhere (other games genuinely should disable input until the next legal-moves update) — not touched. The fix is entirely local to Mines' own tile className; Mines is just the one place this shared, correct mechanism happens to paint across 24 tiles simultaneously instead of one or two buttons.

**PM's implementation, independently verified against the diff — matches exactly.** `opacity-70` dropped from the covered-tile className; `disabled={!clickable}`/`cursor-not-allowed` kept.

D29 is now fully shipped, merged, and deployed. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-17#6 — shipped in PR #644 (bundled with D29), deployed live at `rapidclash-00120-xrb` (confirmed independently, plus a real rendered-DOM dump and a live-page screenshot injection specifically for this ticket — see below). Designer's D28: the win-fill doesn't cover the gem strip, the win text sits inline in the row instead of pinned outside it, and it's the wrong words in the wrong case. Full detail: `ADVISOR_TO_PM.md` 2026-09-17#6.**

- **Items 1-2 (stacking, positioning) — confirmed exactly as diagnosed.** The gem-strip wrapper (`OwnSlot`, `GameHub.tsx`) carries a stray `relative z-10` — above the fill's own `z-1`, so gems paint over the green instead of under it. Fix: drop it (Mines-only by construction, `gemRow` is `undefined` elsewhere). The win text is a normal flex sibling in the row (`shrink-0`, ordinary layout), not the citation's own absolutely-positioned overlay (`right:18px; top:50%`) — fix: pull it out of the row entirely into its own `position:absolute` span, same approach as the fill layer.
- **Item 3 (wrong words/case) — confirmed, with the actual on-screen mechanism worth knowing.** The JSX string is `"You Win"` (wants `"you won"`, lowercase past tense) — but the span ALSO carries Tailwind's `uppercase` class, which force-transforms any text inside it to all-caps via CSS regardless of the literal string — so the string alone wouldn't be enough; the class has to go too. Also swap the font specs for the citation's exact values (bold not extrabold, 16px not 14px, `letter-spacing:0.6px` not `tracking-wide`, `Arial, Helvetica, sans-serif` — this file already has an `ARIAL` constant for exactly this, reuse it).
- **Item 4 — found by checking the citation's own keyframe percentages precisely, not asked for by name.** Fill and text currently share one timing mechanism and fade in at the exact same instant. The citation's own `rcWinText` keyframe holds at 0 through 8% before ramping to 1 by 20% — a deliberate stagger behind the fill's own faster ramp — that today's code doesn't implement at all. Fade-OUT timing is already correctly identical for both. Recommend Framer's own keyframe-array + `times` option for the text specifically to hit the exact cited percentages.
- **The colour change (`#22C55E`→`#16A34A`) is a deliberate reversal of my own earlier, correctly-sourced value from `2026-09-16#7`, applying as directed, not silently.** D28 explicitly names both values and asks for the override — treated as an informed, deliberate decision rather than a misreading. Scope check: doesn't extend to RPS/Blackjack/Chess/Coinflip, which stay on the shared generic green token, matching Dice's own existing special-case rather than a platform-wide unification.
- **Scope, precisely:** item 1 is Mines-only (`gemRow` is Mines-exclusive); items 2-4 fix the SAME shared `OwnSlot` component every game's win-reveal renders through — confirmed intentional via D28's own explicit "compare against dice and rps" check, not scope creep.

**PM's implementation, independently verified against the diff, a real rendered-DOM dump, AND a live-page screenshot — given this is shared, cross-game code, went further than a diff read alone.** The diff matches exactly as scoped: `relative z-10` dropped from the gem-strip wrapper; the verdict text pulled into its own `position:absolute` span; content is the literal lowercase `"you won"`, `uppercase` class removed, font swapped to `font-bold`/16px/`0.6px`/`ARIAL`; the fade-in staggered via a Framer keyframe-array + `times` (`[0,0.08,0.2,0.834,1]`) matching the citation's exact percentages; colour is `winRingColor={gameId === 'dice' || gameId === 'mines' ? '#16A34A' : undefined}` (PM took the one-line simplification I'd flagged as optional). Rendered the actual component via a throwaway test at the win/final state and read the real computed styles off the DOM: fill background `rgb(22, 163, 74)` (`#16A34A`, exact), verdict span `right:18px; top:50%; transform:translateY(-50%); font-family:Arial, Helvetica, sans-serif; font-size:16px; letter-spacing:0.6px`, text literally `you won`, gem-strip wrapper `class="min-w-0 flex-1"` (no `z-10`/`relative`). Then injected that exact real markup into the live deployed page and screenshotted it: green fill fully covers the gems, "you won" renders lowercase and white at the right edge, exactly matching the target.

`grep -rn "YOU WIN"` returns one hit — a code comment describing the old, now-fixed behavior for context, not live text; confirmed the actual rendered string is `you won` throughout.

D28 is now fully shipped, merged, deployed, and independently confirmed at both the DOM and live-page level. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-17#5 — correction to `2026-09-17#4` below, folded into the dispatch, not a separate ticket.** `2026-09-17#3`/D26 merged (`#638`) in the gap between writing D27's ticket and dispatching it. Re-checked the caption fix against the REAL shipped code rather than dispatching the original, now-stale "inherits the release for free" framing: the captions check `resultPhase` directly, not `resultConverge`, and need 3 distinct edits — `ownGemTextVisible` needs `&& resultPhase !== 'closed'` added (own caption fades on dismiss); `oppGemTextVisible` needs NO change (its explicit allowlist never included `'closed'`, so it's already correct by construction); `oppGemRow`'s icon-strip `visible` still needs `'closed'` ADDED (opposite direction — icon strip stays shown, unlike the caption). `resultConverge`'s own redefinition still correctly and automatically handles bar-shift, board-dim, and the VS label exactly as originally described — only the caption/icon-row layer needed the sharper look.

**2026-09-17#4 — shipped in PR #641, deployed live at `rapidclash-00120-xrb` (confirmed independently via `gh pr view` + `git log` + `git show` against the diff). Designer's D27: after the Mines result lands, tapping the board/bars should return them home and un-dim the board while keeping the ring and both gem strips visible — nothing like this exists today. Full detail: `ADVISOR_TO_PM.md` 2026-09-17#4.**

- **Confirmed genuinely missing — the `ResultPhase` state machine has no exit from `'final'` at all**, so the converged, dimmed state is permanent until the next match starts. Fix: add a 5th phase value, `'closed'`, reachable only from `'final'` via a new tap handler.
- **The fix composes cleanly with what's already built, verified by tracing the actual cascade rather than assuming it:** `resultConverge` already drives `barSlideActive` (bar-shift + board-dim) directly — redefining its own computation to exclude `'closed'` releases both automatically, no separate wiring. Once `2026-09-17#3`'s still-unshipped VS-label fix lands (gated on the same `resultConverge`), it inherits the release for free too — worth building both with this in mind together. One thing needs its own explicit fix: the opponent's icon-strip visibility doesn't derive from `resultConverge` and needs `'closed'` added to its own condition directly, since D27 is explicit that strip stays populated after dismiss (unlike everything gated on `resultConverge`, which correctly fades).
- **The dismiss trigger itself: simpler than the prototype's own mechanism, by design, not by accident.** The prototype uses a page-wide overlay with a manual bounding-rect check to keep the Play button clickable through it. Checked our own JSX nesting first: the board, both bars, and the VS label are already all direct children of one shared section, while Play lives in a completely separate one — so a single click handler scoped to that shared section (new opt-in prop, `undefined`/no-op for every hub but Mines) covers every concrete example in D27's own Check list without ever needing to cover Play, or the bottom nav, at all.
- **Two things flagged, not silently decided:** the narrower, section-scoped dismiss (recommended) vs. the prototype's literal whole-screen version, which would also block bottom-nav taps while a result shows — seems unlikely to be wanted but not assumed either way. And D27's closing "same on Dice" note — checked, and Dice has no bar-convergence/board-dim mechanism to release at all (already excluded from it since `2026-09-16#7`), so there's nothing this specific ticket's mechanism applies to there; flagging back rather than expanding scope on one line in a Mines-focused report.

**PM's implementation, independently verified against the diff — matches exactly, including every correction from `2026-09-17#5`.** `'closed'` added as a 5th `ResultPhase`; `resultConverge` redefined to exclude it (bar-shift/board-dim/VS release automatically); `ownGemTextVisible` gets `&& resultPhase !== 'closed'` added; `oppGemTextVisible` left untouched (confirmed already correct by construction); `oppGemRow`'s allowlist gets `'closed'` added (opposite direction, stays shown). The dismiss trigger is the recommended narrower, section-scoped version — a new `onSectionTap` prop, single `onClick` on `hub-section-game`, `undefined`/no-op for every other hub.

D27 is now fully shipped, merged, and deployed. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-17#3 — shipped in PR #638, deployed live at `rapidclash-00120-xrb` (confirmed independently via `gh pr view` + `gcloud run services describe`). Designer's D26: the result reveal is missing its VS label and gem-count captions, and (per Designer) the opponent's gem strip. Full detail: `ADVISOR_TO_PM.md` 2026-09-17#3.**

- **Item 1 (VS label) — real, one-line fix.** The shared `hub-match-vs` element's opacity is gated on `matchForming` only — no awareness of Mines' own result sequence, so it never shows during `converge`/`reveal`/`final`. Fix: extend to `matchForming || resultConverge` (an already-existing, Mines-only prop, `undefined` everywhere else) — mirrors the prototype's own `rpsMatching || mConverged` gate exactly. No position change needed — already centered on the section's own responsive midpoint, which already coincides with where the converged bars land.
- **Item 2 (gem-count captions) — real, the long-tracked `2026-09-12#2` item 3(c), never built until now.** Confirmed absent entirely — a genuinely different mechanism from the already-shipped icon-strip `gemRow` (these sit OUTSIDE the bar box, above/below it). Two precise asymmetries worth getting right: the source's own text generation is asymmetric (opponent's caption never singularizes even at 1 gem; the player's own does) — not a bug to fix, port as-is; and the caption's own visibility timing is NOT the same as the already-live icon row's — the caption only appears once the result sequence starts (`resultConverge`), unlike the icon row which is live from the very start of a round.
- **Item 3 (opponent gem strip) — correction, not a gap: already fully built and test-covered.** Checked directly against the code and an existing PASSING test (`2026-09-16#7`'s own "gem-count rows appear on the sequence's own beats") — the opponent's icon strip already hides through `converge` and fades in at `reveal`, exactly as D26 itself describes. Nothing to build; the "jumps straight to the ring" perception is fully explained by items 1-2 being missing, not this.
- **Converge geometry and final ring/fill — independently re-confirmed already correct**, matching what D26 itself calls "already specified, unchanged." Nothing to do in either area.

**PM's implementation, independently verified against the diff.** Item 1: `hub-match-vs`'s opacity extended to `matchForming || resultConverge` exactly as scoped. Item 2: two new `GameHubProps` slot pairs (`oppGemText`/`oppGemTextVisible`, `ownGemText`/`ownGemTextVisible`), rendered as absolutely-positioned children of the already-relative bar wrappers — confirmed the asymmetric text generation and the differing visibility timing both landed exactly as specified, not smoothed into false consistency. **This exact implementation is what `2026-09-17#5` above re-verifies the D27 dismiss-fix against** — the captions check `resultPhase` directly rather than `resultConverge`, which changed what D27's own fix needs to touch.

D26 is now fully shipped, merged, and deployed. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-17#2 — shipped in PR #635, deployed live at `rapidclash-00119-wkn`. Independently verified via the strongest method available: dumped the ACTUAL rendered HTML from the new code (via a throwaway test render) and injected it into the real, live deployed page to measure and screenshot it directly — not a synthetic approximation, learning from this exact round's own false negative. Both items confirmed fixed, pixel-exact, across 3 repeated fresh page loads. Designer's D25: the icon/halo drift is still present (a different visual signature this time — the pair drifts together, not the icon alone), and the own-bar gem strip fills from the right edge instead of starting after the name. Full detail: `ADVISOR_TO_PM.md` 2026-09-17#2.**

- **Item 1 (icon/halo drift, still) — a genuine second occurrence of D24's own bug, one nesting level deeper than the D24 fix reached; needed live-page verification, not a synthetic repro, to catch.** A synthetic repro of D24's exact shipped structure initially (and misleadingly) measured as correctly centered — the real, deployed page told a different story. D24's fix wrapped the icon's `motion.div` in a new absolutely-positioned wrapper, which correctly fixed the wrapper-vs-tile relationship — but the `motion.div` INSIDE that wrapper is itself still a bare flex item containing a percentage-width SVG, the exact same ambiguous-sizing pattern reappearing one level down. Confirmed via direct DOM injection into the live deployed page and a side-by-side screenshot: current structure reproduces the drift exactly; collapsing "wrapper div → motion.div → svg" down to a single `motion.svg` with `position:absolute` and no offsets (no wrapper divs at all, matching Designer's own literal recommendation) measures pixel-identical centers, consistently across repeated fresh page loads. Applies to all 4 affected `CellKind`s identically (`'safe'`/`'autoSafe'`/`'mine'`/`'bustedOn'`) — none of them were actually fixed by D24, they just hadn't been re-screenshotted yet.
- **Item 2 (gem strip right-aligned) — not a new bug, the direct, now-confirmed consequence of the tradeoff flagged in D24 and left unchanged.** The bar's name span carries `flex-1` (grows to fill the row); the gem-row span after it is `shrink-0` (content-sized) and lands wherever that now-enormous name box ends — the bar's far right edge. Fix, safely scoped to Mines only (since `gemRow` is `undefined` everywhere else): make the name's `flex-1` conditional on `gemRow`'s absence, give the gem-row wrapper `flex-1 min-w-0` instead. Two small, non-blocking spacing notes: the row's shared 10px gap vs. Designer's cited 12px+6px (touches every game if changed, not recommended); `GemCountRow`'s own `maxWidth:90` cap should likely drop now that the wrapper actually has room to grow.

**PM's implementation, independently verified against the diff and the actual live page — matches exactly as scoped.** Item 1: `GemIcon`/`MineIcon` collapsed to `motion.svg` directly (`position:absolute`, no offsets, `animate` targeting the real steady-state opacity directly — no more wrapper-multiplication trick); `GemHalo`/`MineHalo` collapsed to plain `absolute` SVGs, no wrapper divs anywhere. Confirmed via the actual rendered output (not source-reading alone): dumped the real HTML for both a `'safe'` tile and a `'bustedOn'` tile from the new code, injected each into the live deployed page, and measured — both layers land pixel-identical to the tile's own center in every case, repeated across 3 fresh page loads with zero variance. Screenshotted for a direct visual check too: crisp, correctly centered gem and mine, matching the design intent exactly. One flagged tradeoff (the `rcMineJump` CSS animation and Framer's own `animate` now briefly co-touch `transform` on the same element during the ~300ms reveal-pop) — accepted as the right call: the alternative (a separate wrapper) reintroduces the exact bug this ticket exists to remove, and PM correctly judged the overlap low-risk. Item 2: name span's `flex-1` now conditional on `gemRow`'s absence, gem-row wrapper gets `flex-1 min-w-0`, `maxWidth:90` dropped — confirmed via the same live-page injection technique, screenshotted directly: gems now sit immediately after the name, left-aligned, exactly as designed.

Full local suite green (27/27 on `MinesHub.test.tsx`, independently re-run), CI green per PM's report, bot-crowd restarted and holding.

D25 is now fully shipped, merged, deployed, AND independently confirmed against the real live page for both items — closing out the two-round icon-centering saga (D24 fixed one layer, D25 fixed the layer underneath it) with actual pixel measurement, not just code review. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-17#1 — shipped in PR #631 (items 1+3) and PR #632 (item 2), deployed live at `rapidclash-00118-b2t` (confirmed independently via `gh pr view` + `git log` + `git show` against both diffs, and `gcloud run services describe` — matches PM's report exactly; also independently re-ran the full `MinesHub.test.tsx` suite locally — 27/27 green, matching PM's own claim). Designer's D24: gem/mine icons drift off-centre from their halos, the post-bust result sequence waits for the opponent instead of running on the player's own clock, and the own-side gem count should update live as tiles are tapped instead of only appearing at the result. Full detail: `ADVISOR_TO_PM.md` 2026-09-17#1.**

- **Item 1 (icon/halo drift) — confirmed real; Designer's stated mechanism doesn't match the code, but the suggested fix works anyway, confirmed empirically.** Not a `top:0`/`inset:0`/`translate` literal needing removal (checked — none exist where Designer's diagnosis implies). The real cause, proven with an isolated repro of the actual tile structure: `GemIcon` (a `'safe'` tile) is a bare, unwrapped flex child, and a normal-flow flex item containing a percentage-width SVG hits a genuine CSS ambiguity — Chromium stretches the flex item to the container's full width (leaving `justify-content:center` nothing to center), and the SVG then sits flush at the start of that box. `GemHalo`'s existing wrapper (`absolute; inset:0` + internal flex-centering) sidesteps this and already measures dead-center. Scope is narrow: only `'safe'` and `'autoSafe'` `CellKind`s are affected — `'mine'`/`'bustedOn'` already use the same wrapper technique (added for `2026-09-16#6`) and are already correct. Fix: wrap the two affected icons in the same `absolute inset-0 flex items-center justify-center` div already used 3 other places in this file (Designer's own literal "bare SVG, position:absolute" also verified to work, if a smaller diff is preferred).
- **Item 2 (result runs on the opponent's clock) — real, and the most consequential item: traces to `2026-09-16#7` over-applying the honesty principle.** Last night's fix gated the ENTIRE sequence — including `'converge'`, which reveals no data at all (just a board-dim + bar-slide) — behind full match settlement (both players locked), when only the number/outcome-revealing beats (`'reveal'`, `'final'`) actually need that. Fix: trigger `'converge'` off the player's OWN `locked` flag (already in the view shape, flips true on any of the 3 real ways a round ends — bust/clear/timeout — independent of the opponent), with a reason-based delay (1500ms bust, 500ms timeout, both derivable from already-stable view fields, no protocol change needed) — then hold at `'converge'` if the opponent hasn't finished, resuming `'reveal'`/`'final'` at the same +820/+700 spacing once they do. **A real consequence found by tracing how the pieces connect, not in Designer's own brief:** `holdResultMs` (the delay before the win/lose overlay popup) needs to become a dynamically-computed remaining-time value instead of the fixed 3020ms constant it is today, or the popup will appear far too late in the hold case. One open question flagged, not guessed: the prototype has no "cleared all 22" code path to confirm a timing against — recommend defaulting a clear to the timeout's 500ms, flagged for confirmation.
- **Item 3 (live own-gem-bar) — real, mostly just removing an over-restrictive gate.** `ownGemRow` is currently gated behind `didBust`, so it only ever appears after the round ends. Fix: drop that gate — always pass the live count; the existing `count <= 0 → null` check already produces the "fades in with the first gem" behavior. One already-considered tradeoff flagged for reconsideration rather than silently decided either way: Designer's citation asks for `flex:1 1 auto` on the gem strip, but `GameHubProps.oppGemRow`'s own doc comment already rejected that last night (would break long-name truncation on every OTHER hub sharing this bar) — since `gemRow` is Mines-only, this can likely be done safely by moving `flex-1` conditionally, only when a gem row is actually present.

**PM's implementation, independently verified against both diffs — all 3 items landed exactly as scoped, plus two things worth recording precisely:**
- **Item 1/3 (PR #631):** `GemIcon` in both `'safe'`/`'autoSafe'` wrapped in the same `absolute inset-0 flex items-center justify-center` div already used elsewhere — confirmed against the diff, DOM order preserved (icon wrapper still after halo, keeping the D23 stacking fix intact). `ownGemRow` now unconditional. **A more precise correction than what I asked for:** I'd suggested a single 320ms→280ms fix; PM re-checked BOTH the opponent's and the player's own citations independently and found they're genuinely DIFFERENT prototype values (opponent stays 320ms, own row is 280ms) — not the same number miscopied twice. Implemented via a new `transitionMs` prop, opponent's default unchanged. Kept `shrink-0` as-is rather than the conditional-`flex-1` reconsideration — a reasonable call given it was explicitly left as PM's own choice.
- **Item 2 (PR #632):** the corrected state machine (own-lock trigger, reason-based delay, hold-and-release keyed on `opp.locked`) matches exactly as scoped. `holdResultMs`'s dynamic formula is a genuine improvement on what I worked out — PM caught that a stateful "release" ref would race against `GameHub`'s own consuming effect (child effects fire before a parent's own) and replaced it with a pure per-render computation instead, provably race-free, confirmed reducing to the exact old 3020ms constant in the straight-through case. **A significant, correctly-identified scope consequence, not a side effect:** the fix generalizes to ALL lock reasons (bust/cleared/timeout) per the ticket's own text, not just bust — meaning every Mines match ending now gets the full converge/reveal/final choreography instead of the old instant-reveal path for non-bust endings. Checked this against the prototype's own source directly: the prototype's `startMinesResult()` is called identically for BOTH the bust and clock-timeout paths, with no "instant" case anywhere — so this generalization makes production MORE faithful to the design, not less; recorded here specifically so it's never mistaken for a regression if a future Designer report calls the win animation "slower."
- **The 'cleared'-timing default (500ms, same as timeout) is recorded in code as "Owner confirmed, 2026-09-17"** — this reached PM directly rather than through this mailbox; noting it here for the record since it settles the one open question this ticket flagged.

D24 is now fully shipped, merged, and deployed. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-16#7 — shipped in PR #628, deployed live at `rapidclash-00117-wvm` (confirmed independently via `gh pr view` + `git log` + `git show` against the actual diff, and `gcloud run services describe` — matches PM's report exactly). Items 4-5 from D23, scoped as their own ticket: the post-match bar-convergence sequence + correct ring/fill colours, and the gem-count icon rows — closing out `2026-09-12#2`'s long-open items 3(a)/3(c). Full detail: `ADVISOR_TO_PM.md` 2026-09-16#7.**

- **Self-caught correction, before anything was built on it:** my own `2026-09-16#6` dispatch cited the win-ring colour as `#16A34A` — wrong. That's Dice's own special-cased `DICE_WIN_GREEN` (already correctly Dice-only per `2026-09-16#4`), not Mines'. Mines has no per-number recolor at all — its only win/lose/draw tell is the shared `playerBarRing`/`winFillAnim` mechanism, whose own literal (re-read directly) is unambiguously `#22C55E` win / `#FF3E5E` loss / `#FF8A1E` draw.
- **Item 4 (bar convergence) — the transition mechanism is already fully built and reusable; only the trigger + magnitude are missing.** `GameHub.tsx`'s existing `barShiftY` prop already applies the exact `transform:translateY()` + `620ms cubic-bezier(0.3,0.9,0.32,1)` transition the prototype uses for this — confirmed character-for-character. Needs: a new Mines-scoped local trigger state (not the shared `barSlideActive`, which means pre-match only) firing at the prototype's own exact timing (bust → +1500ms converge → +2320ms counts reveal → +3020ms ring lands), recommended built the same way as D21's own reveal state machine; a live re-measurement at the convergence point rather than a hand-tuned second fallback constant (the prototype's own different fallback for this phase is best explained as what a fresh measurement naturally produces, not a number to copy literally); and extending the existing board-dim (`MinesPanel`'s `args.barSlideActive ? 0.28 : 1`) to also cover this new state.
- **Ring/fill colours — genuinely missing for Mines today, same class of gap Dice had before its own fix.** Confirmed Mines gets none of the `lossRingColor`/`winRingColor`/`winFillColor` overrides (all `gameId === 'dice'`-only today) and falls back to generic shared colors that don't match the prototype's own values for this mechanism at all. Fix: extend the same props to Mines with Mines' own correct literals (reusing the already-existing `--rc-loss` token for loss; win and draw need new values, draw needs a new override point entirely since none exists yet).
- **Item 5 (gem-count icon rows) — confirmed real, matches `minesOppGemList`/`minesGems` exactly, but depends on item 4's state machine rather than being independent** — its own mount/fade conditions key off the same phase states item 4 introduces. Recommend building item 4 first, then hanging item 5 off its beats. One real asymmetry to preserve: the opponent's row genuinely fades in; the player's own row's opacity is a flat constant that never changes despite carrying a transition — don't make both sides fade identically. The separate, still-open `oppGemText`/`myGemText` plain-text mechanism (flagged in `2026-09-16#6`) remains unbuilt and un-confirmed with Designer, not part of this ticket.

**PM's implementation, independently verified against the diff — a real, sound design decision worth recording, not just noted.** PM anchored the whole converge/reveal/final sequence on the SERVER having confirmed the terminal result (`lastOutcome && lastSettlement`, matching `GameHub`'s own `hasFreshResult` gate) rather than on the player's own local bust — because in a real multiplayer match, busting doesn't mean the match is over (the opponent could still be mid-round), and the server alone knows when the result is truly final. Triggering purely off the local bust risked revealing a count before the server confirmed it — exactly what D21's honest-data principle rules out. Same relative pacing as the prototype (1500/2320/3020ms), just measured from a moment that's always backed by real, confirmed data instead of the prototype's own client-simulated instant. Scoped strictly to the bust path, as ticketed — every other match-ending path keeps its existing mechanism, untouched. Confirmed in code: `MinesHub.tsx`'s `RESULT_CONVERGE_MS`/`RESULT_REVEAL_MS`/`RESULT_FINAL_MS` timers only arm once `didBust && props.lastOutcome && props.lastSettlement`.

**Ring/fill colours confirmed correct in the diff:** `GameHub.tsx` now passes `winRingColor`/`winFillColor` = `#22C55E` and a new `drawRingColor` = `#FF8A1E` for `gameId === 'mines'` specifically (Dice keeps its own separate `#16A34A`), `lossRingColor` extended to `var(--rc-loss)` for both. A new opt-in `drawRingColor` prop was added to `OwnSlot`, the same shape as the existing `lossRingColor`/`winRingColor`. PM also independently caught and fixed the same colour mistake before merging — had mis-cited `#16A34A` for Mines in a first pass, corrected to `#22C55E` per the correction above, without needing a second round-trip.

D23 is now fully closed — all 5 items shipped, merged, and deployed; `2026-09-12#2`'s items 3(a)/3(c), open since way back, are finally closed too. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket or anywhere in the D23 batch.

---

**2026-09-16#6 — items 1-3 shipped, merged (PR #625), and deployed live at `rapidclash-00117-wvm`; items 4-5 shipped, merged, and deployed too, see `2026-09-16#7` above. Designer's D23: when a player hits a mine, the glow renders on top of the bomb icon instead of behind it, the rest of the board never reveals, and the hit tile doesn't bounce; plus the exact timing for the post-match bar-convergence sequence and the gem-count icon rows in both bars. Full detail: `ADVISOR_TO_PM.md` 2026-09-16#6.**

- **Item 1 (glow-on-top) — confirmed real, and empirically proven to be a genuinely different mechanism than Designer's own stated cause.** The current code's own DOM order is already halo-then-icon (matches Designer's literal ask) — so "reorder the JSX" would have been a no-op. Built and screenshotted an isolated repro of the real 4-SVG tile structure to settle this properly rather than guess: the actual cause is that the icon SVGs have no `position` set while their halo siblings are `position:absolute` — CSS always paints a positioned sibling above a static one regardless of DOM order. The repro reproduced the exact red-smear bug with the current structure, and the correct crisp-icon-with-glow-behind-it once `position:absolute` was added to the icon. Traced to a real, narrow gap: the prototype's own bomb icon IS `position:absolute`; its gem icon is not (harmless there, since the halo is low-opacity and shape-matching) — our port copied the gem icon's lack of positioning onto the mine icon too, which the prototype never does.
- **Item 2 (board doesn't reveal) — confirmed real, one missing branch.** `cellKind()` only ever returns non-`'covered'` for the busted tile, tiles you personally uncovered, or (once locked) actual mines — any untouched, non-mine tile stays `'covered'` forever, even after a bust. The prototype's own `open = picked || busted` makes ALL 25 tiles open the instant a bust happens. Needs a new `CellKind` state (ghosted/auto-revealed-safe) for this fallback case.
- **Item 3 (the bounce) — confirmed genuinely missing.** `rcMineJump` has zero occurrences anywhere in the codebase; its exact 14-step keyframe matches the prototype's own source character-for-character. Same add-a-keyframe pattern already used for Dice's `rcPlayShake` (`2026-09-16#4`/`#622`).
- **Items 4-5 are not new asks — they're `2026-09-12#2`'s own long-open items 3(a) and 3(c), finally given exact numbers.** Item 4 (bars converge post-hit) is 3(a) — `GameHub.tsx`'s `barSlideActive` is still only `matchForming || searching` today, no post-match state exists; D23 supplies the missing timing (bust → +1500ms converge over 620ms → +820ms counts reveal → ring lands at +1520ms). Item 5 (gem-icon rows) is 3(c)'s icon-row half — confirmed real, matches `minesOppGemList`/`minesGems` exactly (capped at 22). **A genuinely separate, still-open piece flagged, not silently dropped or conflated:** the prototype ALSO has `oppGemText`/`myGemText`, plain text spans positioned entirely outside the bar box — a different element from the icon row, from the same original 3(c) finding, that D23 doesn't mention at all. Recommend building the icon row now and confirming with Designer separately on whether the text labels are still wanted.
- **A real asymmetry to preserve when building item 5:** the opponent's gem row genuinely fades in (a real state toggle); the player's own row's opacity is a flat constant that never changes despite having a transition on it — match this asymmetry, don't make both sides fade identically.
- **Correction:** the ring colours mentioned in this entry's original draft (`#16A34A` win) were wrong — see `2026-09-16#7` above for the corrected values (`#22C55E` win) and the full items-4-5 ticket.

**PM's report on items 1-3 (PR #625), independently verified via `git show`:** MineIcon (and GemIcon's now-explicit opacity prop) get `position:absolute` where a halo sibling exists — exactly the fix scoped, confirmed against the diff, not just PM's own description. `cellKind()`'s fallback now returns a new `'autoSafe'` kind once the player is locked. `rcMineJump` added to `styles.css`, applied via a plain CSS-animated wrapper kept deliberately separate from the Framer-driven reveal-pop `motion.div` — PM's own call, sound reasoning (keeps the CSS keyframe and Framer's transform animation from fighting over the same property), not something I'd scoped but a correct extension of the ask.

D23 is now fully shipped, merged, and deployed end to end — items 1-3 here, items 4-5 in `2026-09-16#7`.

**Advisor next:** available, no open thread. **PM next:** nothing pending anywhere in D23.

---

**2026-09-16#5 — shipped, merged (PR #624), and deployed live at `rapidclash-00117-wvm` (confirmed independently). Designer's D22: strip a duplicate status row + explainer paragraph + Resign button (none exist in the design) from the Mines board, and verify every cited colour. Full detail: `ADVISOR_TO_PM.md` 2026-09-16#5.**

- **Item 1 (the clutter) — confirmed real, safe to delete, one careful exception.** `MinesBoard`'s own internal "You/Opponent" row, status line, explainer paragraph, and Resign button have zero equivalent in the prototype — the shared `GameHub` bars already show the real avatar/name/"Playing…" for free once this duplicate is gone. Confirmed via the exact grep D22 asks for: only one "resign" hit anywhere in Mines' code, the button's own text. **The exception:** `onForfeit`/`handleForfeit` is fully generic, shared, cross-game infrastructure still legitimately used by RPS/Chess/Blackjack's own real Resign buttons — deleting it would break three other games. The Mines-specific server-side `forfeit()` handler becomes practically unreachable once the button's gone, but it's a required member of the shared `GameModule` interface every game implements — recommended leaving it in place (dead-but-harmless) rather than restructuring a shared interface for one function. Also noted, not touched: a separate, pre-existing, already-known dead-code cluster (`MinesPlay.tsx`'s own old Resign button, part of the already-flagged "lobby/stake-entry/play/result" backlog item) — unrelated to this ticket.
- **Item 2 (colours) — confirmed every single cited value already matches exactly.** All 7 rows of D22's table checked directly against the file's own constants — nothing needs changing. This file's earlier T8 visual rebuild was already pixel-perfect here.
- **Item 3 (structure) — confirmed every cited layout value already matches exactly** (card/board/tile padding, radii, gaps). The matchmaking-dim is already correct; the separate "result-converge" dim D22 also mentions is an already-known, already-tracked gap from `2026-09-12#2` item 3(a) — not re-opened here.
- **Item 4 — a real, confirmed gap found by deriving the exact timer-collapse condition D22 asked me to check.** The 1000ms linear drain is already correct. The collapsing wrapper is missing entirely — the timer stays visible through the whole result-hold window today instead of collapsing, confirmed by checking the prototype's own exact gating condition.
- **Item 5 — beyond D22's own list: the tile-reveal has zero animation today.** The tile's own cited transform is very likely inert in the prototype itself (a constant value that never changes) — not worth porting literally. The REAL reveal pop lives on the gem/bomb icon itself (0.4→1 scale, 0→1 opacity, matching an established convention used identically for RPS's own icon reveals) — completely absent from our code, which just renders the icon instantly with no transition at all.
- **Item 6 — small, low-priority:** the tile's own background-color transition duration doesn't quite match Tailwind's default vs. the cited 200ms — cheap fix, low priority.
- **Item 7 — found while verifying D22's own "Playing…" claim, but not Mines-specific.** The shared `OpponentSlot` fallback text has real font-weight/case/letter-spacing mismatches against the prototype's own citation — affects every game using the fallback (confirmed Dice too), not just Mines. The colour half is part of the already-tracked, deferred `GameHub.tsx` light-theme backlog from `2026-09-15#9` — not re-opened. The weight/case/tracking half is separate, small, and wrong in both themes today — cheap to fix now, recommended alongside this ticket since checking D22's own claim is what surfaced it.

**PM's report (PR #624), independently verified via `git show`:** all 7 items landed as scoped — item 1's deletion (status row/paragraph/Resign button gone, `onForfeit` itself untouched), item 4's RoundClock collapse, item 5's icon reveal pop, item 6's transition-duration fix, and item 7's shared `OpponentSlot` weight/case/tracking correction — plus new regression tests for 1/4/5/7, full suite green. Items 2-3 re-confirmed already correct, no change, matching the original ticket.

D22 is now fully shipped, merged, and deployed. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-16#4 — shipped in PR #622, deployed live at `rapidclash-00116-5d9` (confirmed independently via `git log`/`gh pr view` and `gcloud run services describe` — matches PM's report exactly).** Not a Designer bug report — Designer submitted a full technical animation spec (`design-ref/D21`) because a plain description couldn't capture what was missing; Owner then made the actual product call in a direct conversation. Full detail: `ADVISOR_TO_PM.md` 2026-09-16#4.

- **PM's report:** item 1's reveal state machine shipped as designed (reusing `BlackjackHub`'s `onRevealComplete`/`gateResultOnReveal` pattern); items 2-4 (sound sequencing, resting-state fix, WIN ring/fill color) shipped alongside it. PM also found and fixed two things independently: a state-pairing race condition, and the root cause of a pre-existing flaky test.

- **The core finding:** the prototype's Dice reveal is theatre — a 444ms client-side counting animation, then a staged ~1.8s hold before the ring/fill/sound land. Our honest, server-authoritative reveal has never replicated this; it snaps straight to the final state the instant data arrives. This directly touches the platform's standing "never fabricate data" principle, so it was raised with Owner directly rather than guessed at.
- **Owner's explicit, decisive resolution:** the honesty principle stays fully intact — never display a value that could turn out wrong, never invent an event that didn't happen — but once the server's real result is known, we're free to spend time revealing it dramatically, matching the prototype's own choreography exactly. Counting up to an already-known-true number is theatre, not fabrication; today's code conflated the two.
- **Item 1 (the real one) — a genuine new client-side reveal state machine**, but built on an already-proven, already-shipped mechanism: `BlackjackHub`'s own `onRevealComplete`/`gateResultOnReveal` (paces a locally-computed reveal timer, then signals the shared hub once the last beat lands) — Dice needs the identical wiring, not new GameHub plumbing. The exact timeline (0/460/904/1804ms) is D21's own, reusing the prototype's real eased count-up formula fed the REAL final numbers as the animation target — nothing shown mid-count is ever a value that could turn out wrong.
- **Item 2 — sound sequencing**, a side effect of the same redesign: today both `dice-roll` and `dice-win` fire in the same instant (the moment data arrives) — exactly the "everything happens at once" problem described. Moving them to the reveal timer's own 460ms/1804ms beats fixes this with no new sound-engine work.
- **Item 3 — a real, independently-confirmable resting-state bug found while deriving this:** the cube's own null-fallback position (0, left edge) and the fill bar's null-fallback width (50%) don't agree with each other — visible for the entire wait on the server today. The prototype avoids this because it flips its local value to 0 the instant a round starts; our code has no equivalent step. This is naturally item 1's own "t=0" beat, not a separate fix, though independently shippable early if useful.
- **Item 4 — unrelated, ships regardless:** Dice's WIN ring/fill was never given the same color fix the LOSS ring got in `#612` — it still resolves through the shared `--rc-green` token (`#34d399`/`#0b8f5a`), not the exact `#16A34A` both the original ticket and D21 specify. Missed because prior attention was entirely on the loss side. Same fix shape as the existing `lossRingColor`.
- **Confirmed already correct, extensively:** colors, geometry, cube SVG (all 3 faces, verified against the prototype's actual source), belt sizing/colors/edge-fades, and the win-fill's own timing (500/2000/500ms = 3000ms, exact match).
- **Explicitly not re-opened:** matchmaking's simulated search timing and the 70ms scramble cadence (both already-accepted divergences from `2026-09-15#9`). The tie/draw ring color question (open since `2026-09-15#13`) is confirmed **genuinely moot for Dice specifically** — the game engine auto-replays on an exact tie rather than ever surfacing one as a terminal, visible result.

D21 is now fully shipped and deployed. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-16#3 — RESOLVED, comment-only, no ticket needed.** `2026-09-16#2`'s "separate finding" (the `lib/currency.ts` default-value mismatch against the prototype) turned out to be an already-documented, deliberate decision — `CurrencyPicker.tsx`'s own comment already records, by ticket number (`2026-09-11#5`), a deliberate call to default to `'USD'` instead of the prototype's `'SOL'`, specifically so the wallet trigger shows the user's real balance by default. `lib/currency.ts`'s own comment just cited the wrong reason (claimed it matched the prototype, when it deliberately doesn't). Fixed the comment directly to point at the real decision — no behavior change, `DEFAULT_CURRENCY` stays `'USD'`. Full detail: `ADVISOR_TO_PM.md` 2026-09-16#3.

**Advisor next:** available, no open thread. **PM next:** nothing pending — this was informational, already closed.

---

**2026-09-16#2 — shipped and merged in `#617`.** Designer's D20: the bet-amount row on every game screen shows USD/a dollar icon regardless of the wallet's actual selected currency. Full detail: `ADVISOR_TO_PM.md` 2026-09-16#2.

- **Confirmed real, fixed exactly as scoped** — 3 literal `"USD"` values in `GameHub.tsx`'s currency row swapped for the existing `useCurSel()` singleton already proven on `HubRibbon`/`GamesCarousel`/`RewardsHub`. Guest mode's RC-icon/`'RC'`-label branch left untouched, confirmed via diff. One shared `GameHub.tsx` component covers rps/mines/dice in one fix.
- **The "separate finding" flagged alongside this ticket (the default-currency question) resolved — see `2026-09-16#3` above.** It was a documentation fix, not an open product question; no code change needed.

**Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-16#1 — CLOSED, fixed in `#615`.** PM's `#612` report had said Dice's `DICE_LOSE_RED` (#DC2626) was "already correct" and left it untouched — that was wrong; D19's own text is explicit that the cube number changes to #FF3E5E too, not just the bar ring. Full detail: `ADVISOR_TO_PM.md` 2026-09-16#1.

- **Root cause, acknowledged precisely by PM:** citation-verification (confirming what the prototype currently does) got treated as equivalent to confirming ticket intent (what Designer wants changed) — those are different questions, and here they pointed opposite ways.
- **Fixed:** `DICE_LOSE_RED` now reads `var(--rc-loss)` instead of the literal `#DC2626`, exactly as recommended (referencing the same token the ring already uses, not a second hardcoded literal). A test now asserts the losing cube's own rendered color.

D19 is now fully closed — all of item 1, the ring, and the cube number are shipped and correct. **Advisor next:** available, no open thread. **PM next:** nothing pending on D19.

---

**2026-09-15#13 — shipped in `#612`, merged; not yet deployed (PM held it, checking with Owner on timing). One follow-up open, see `2026-09-16#1` above.** Designer's D19: the bet-warning ring wraps the whole panel instead of just the bet-track pill, plus a correction to Dice's loss colour (`#FF3E5E`, not the `#DC2626` D17 used). Full detail: `ADVISOR_TO_PM.md` 2026-09-15#13.

- **Item 1 — confirmed real, clean, ready to ship.** The ring (`GameHub.tsx:1258`) is on the whole `hub-section-bet` wrapper, not the bet-track pill — matches the screenshot exactly. Needs: the ring rescoped + converted to an animatable `boxShadow`, the hint text changed from "Select a bet amount to play" to the prototype's exact "Choose your bet" in the new red (not `--destructive`/`--rc-danger`), the text's instant `sr-only` toggle replaced with a real `max-height`/`opacity` collapse, and a genuinely missing `rcPlayShake` keyframe added (confirmed zero occurrences anywhere in the codebase) and wired to the PLAY button on the same press.
- **Two things already correct or deliberately not ported:** `BET_TRACK_BG` already matches the prototype exactly. The prototype's manual scroll-offset formula (with its "only add 33 the first time" special case) isn't needed here — this app already uses the browser's native `scrollIntoView`, which re-measures live layout every call and has no stale-offset problem to compensate for. Recommend keeping it as-is.
- **Item 2 — the token and Dice's own cube-number constant are safe, mechanical changes.** `#FF3E5E` doesn't exist as a token yet and needs to be added fresh — confirmed it's distinct from both `--rc-danger` (a different, theme-varying red used in 4 unrelated screens) and `--destructive` (the pink-red currently backing both the bet-warning ring AND the shared loss-ring mechanism); neither should be repurposed. `DiceHub.tsx`'s `DICE_LOSE_RED` constant is a trivial one-line swap.
- **A real decision reversal flagged, not silently implemented:** the player-bar loss ring still needs the same Dice-scoped `OwnSlot` plumbing already identified in D17 (it's shared with 5 other games) — but this ticket ALSO asks for a loss FILL, directly reversing Owner's same-day D17 answer ("keep ring-only, no new fill feature," made specifically because the prototype has no loss fill either). Recommend an explicit Owner reconfirm before building the fill specifically — the ring-color and cube-number changes have no such conflict and are safe to ship regardless.
- **A scope-reading flagged, not assumed:** treating "#FF3E5E everywhere" as Dice-only (matching D17's own explicit "everywhere on THIS SCREEN" scoping), not a request to repaint every other game's own shared loss ring — flagged the alternate reading rather than picking one silently.
- **"Funds warning" is contextual citation only** — confirmed nothing like it exists in this app today; not part of this ticket's build scope.

**Advisor next:** available, no open thread. **PM next:** ship item 1 + item 2's mechanical parts now; hold the loss-fill overlay specifically for an Owner reconfirm.

---

**2026-09-15#12 — shipped in `#608`, deployed live at `rapidclash-00114-gbp` (confirmed independently via `gcloud run services describe`, matching PM's report exactly).** Designer's D18: the Related Games section needed a full rebuild — nothing in the prior version matched the prototype. Full detail: `ADVISOR_TO_PM.md` 2026-09-15#12.

- **Full rebuild shipped** — heading (bolt icon + standard headline style), two arrow buttons (safe `popId` press-pop, exact `nudgeRel`/mute-threshold math), snap-scroll rail, correctly-shaped cards with plain tile-art fills. PM independently re-verified every citation before implementing, including one detail worth double-checking that held up exactly: the mute threshold is strictly `scrollLeft > 20` for "active," matching the prototype's own source precisely.
- **`relatedGamesFor(currentGameId, liveIds)` added to `gameSort.ts`** — the one genuinely new piece, a pure function unit-tested directly against all 3 of Designer's own worked examples (Mines/Dice/Chess) plus edge-case coverage (no dupes, current game always excluded, correct `playable` flag on both head and tail slots) — 6 tests, all green on the first attempt.
- **A real, non-regression consequence surfaced and fixed:** building "Crash always shows, dimmed" correctly means every related-rail tile now mounts immediately (dimmed) before the live `/games` fetch resolves, rather than not existing in the DOM pre-fetch at all — the intended generalization of Designer's own requirement. This broke 3 existing tests that assumed the old absent-until-confirmed timing; fixed by waiting for real playable state instead. **One of the three was a previously-flagged-flaky test — it shared this exact root cause and is now deterministic, not just fixed by coincidence.**
- **The stale `COMING_SOON` 'dice' entry** flagged in the ticket was cleaned up in the same PR.
- **Closing ask (harness capture)** — already done at ticket time, no further work needed.

Deployed at Owner's request — took unusually long (~20+ min stuck on source upload specifically) but completed clean, no retry needed. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-15#11 — Advisor correction (informational, nothing to dispatch): re-checked the `2026-09-15#10` item-1 arithmetic more carefully and it doesn't hold up at the size originally claimed.** A faithful static reconstruction of `DiceBoard`'s exact CSS values, measured in real Chromium, showed only ~1.2px of belt-clipping overflow with the status text present — not the ~10px the original source-level estimate claimed (the original math used unmeasured, approximate intrinsic heights for `DiceScaleRow`/the status `<p>`). **The shipped fix (`#605`, removing the status text) is still unambiguously correct** — real leftover copy, zero prototype occurrences, can only help the belt's available space — **but a 1px clip is very unlikely to be the FULL explanation for "plain bars with no numbers."** A more likely secondary contributor, not yet checked: the belt's pills mount via Framer Motion (`initial={{scale:0.45, opacity:0}}`, 460ms animation) — a screenshot caught mid-animation would look exactly like an unlabeled bar, which is normal behavior, not a bug. Couldn't safely stage the actual live 2-player match needed to settle this definitively (would need new WS-protocol scripting, more infrastructure than this single visual-QA point justifies). **The live pill-sizing spot-check PM asked for stays genuinely open** — worth a glance next time anyone's watching a real resolved Dice round, not worth either side building new live-match test infrastructure for right now.

**2026-09-15#10 — NEW, top item. Designer's D17: the Dice result state shows leftover "You rolled higher!" text covering the history belt, plus wrong ring/fill colors on the player bar. Full detail: `ADVISOR_TO_PM.md` 2026-09-15#10.**

- **Item 1 — shipped in `#605`, deployed live at `rapidclash-00113-2jv`.** The result text (`DiceHub.tsx:316-318`) was a real, exact match to Designer's citation (zero occurrences in the prototype). The separate "pills show no numbers" complaint wasn't a missing label — the belt already rendered it correctly — the original theory was a layout overflow the removed text was contributing to. **Advisor later re-checked that arithmetic more carefully and corrected the estimated overflow size downward (~1px measured, not ~10px) — see `2026-09-15#11` above.** The fix itself stands regardless; the live pill-sizing spot-check remains open.
- **Item 2 — needed zero code.** Owner's answers to both open questions (loss-fill: keep ring-only, matching the prototype exactly, no new fill feature; draw ring: keep the shared amber) both landed on "keep current behavior" — nothing to build.
- **Closing ask (harness capture for the result state) — done.** Added a `dice-result` screen definition to the design-fidelity tool, reusing the harness helpers already built for D16, and verified both theme outputs directly.

Deployed live alongside `#602` in the same batch deploy, per Owner's timing call. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-15#9 — shipped in `#602`, deployed live at `rapidclash-00113-2jv`.** Designer's D16: the opponent bar during matchmaking showed the wrong layout (no blur, no scramble, name inline with "Searching…" instead of split right). Full detail: `ADVISOR_TO_PM.md` 2026-09-15#9.

- **All 4 structural complaints confirmed real, in the ONE shared component every game already routes through** (`OpponentSlot`, `GameHub.tsx`) — so "same on rps, mines and dice" is automatic, not something needing separate verification per game.
- **Layout, blur, avatar hashing, and the robot-emoji leak all shipped** — all 4 confirmed real, all fixed in the one shared `OpponentSlot` (`GameHub.tsx`), applying automatically to RPS/Mines/Dice. PM independently re-verified every citation before implementing (the exact layout, the prototype's two-piece split, the hash/`PRESETS` shape, `displayHostName`'s exact regex) — all matched.
- **Avatar hashing (item 3):** `avatarIdForName(name)` added to `Avatar.tsx` — same djb2 hash already used for `discColor`/`glyphColor`, applied to the preset id set instead of a hue. The one genuinely new piece of logic, as expected.
- **Robot-emoji leak (item 4):** `displayHostName` exported from `GamesCarousel.tsx` and reused directly rather than duplicated. One subtlety PM got right that wasn't spelled out in the ticket: the self-comparison (excluding the player's own name from the scan) stays on the RAW `ownerName` — only what actually gets added to the displayed scan set is stripped, so a human player's own username (never bot-prefixed) can't mismatch against a stripped form of itself.
- **Already correct, confirmed, no changes needed:** the muted→bright name-color transition, and no tier icon during search.
- **A new, genuine finding surfaced while implementing item 5's "already correct" claim:** the color-transition LOGIC is right, but the underlying tokens (`text-foreground`/`text-muted-foreground`) have zero light-mode override anywhere in `index.css` — the same broken-token class already fixed this week on `HubToolbar`/`HubRibbon`/`HubFooter`/`GamesCarousel`. PM checked the whole file: **17 occurrences of this exact pattern in `GameHub.tsx`, not just the 2 in `OpponentSlot`** — the file has never had a light-theme pass at all. Deliberately left untouched (out of scope for D16, and too big to fold in casually — same shape/size as the repo-wide hex sweep already flagged from `2026-09-15#2`). **Recorded here as its own open follow-up, no ticket number yet, no timeline pressure.**
- **Closing ask (harness capture for the searching state) shipped alongside the ticket, done, not just requested.** Added a new `rps-searching` screen definition to `tools/design-fidelity/src/screens.ts`, ran the actual capture, and visually verified both theme outputs match Designer's own reference exactly — blurred flickering avatar, blurred scrambling name, "Searching…" alone on the right. One small timing correction found along the way: the search/blur/scramble start synchronously at t=0 on PLAY, not after the ticket's stated 680ms (that 680ms is the first leg of the nested delay before 'found' fires, at 2380ms total).

Deployed live at `rapidclash-00113-2jv` alongside D17's item 1 fix, per Owner's timing call. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket.

---

**2026-09-15#8 — NEW, top item, NOT ready to ticket — needs a design decision before any code is written. Designer's D15: the Rakeback CLAIM button looks two-toned (lighter face, darker ledge) when dimmed. Full detail: `ADVISOR_TO_PM.md` 2026-09-15#8.**

- **Designer's specific mechanism claim doesn't match the code.** The described bug (opacity applied to the face only, missing the box-shadow ledge) isn't what's happening — `opacity` is already set once, on the single `<button>` that owns both `background` and `boxShadow`, confirmed unchanged since `#570` (2026-09-13, two days before this report). CSS opacity composites an element's entire rendered box as one group; there's no way for a browser to apply it to a background fill while skipping that same element's own box-shadow. Re-applying "opacity on the outer element" would be a no-op — it's already there.
- **The real mechanism: two intentionally different purples, dimmed together.** The face (`--brand-purple`, `#8B45F0`) and the ledge (`--rc-theme-toggle-active-shadow`, `#5F27B8`) are deliberately different shades — the same lighter-face/darker-ledge 3D-button convention used on JOIN, PLAY, and WALLET elsewhere in the app. The absolute hue gap is identical at full opacity and at 50% — dimming doesn't change it — but it becomes more visually prominent as a "seam" once both colors sit closer to the neutral surface.
- **The Volume Bonus comparison doesn't hold up as a reference.** Confirmed: Volume Bonus's CLAIM is *never* the purple pill — it unconditionally renders the shared, flat, single-hue `LockedClaimRow` in every state (an explicit design decision, issue #435, since Volume Bonus has no independently-claimable balance of its own). It looks uniform because it's structurally a different, single-hue component, not proof the purple button can be made uniform by fixing an opacity bug.
- **No prototype reference exists for this at all.** Checked the prototype's own CLAIM markup (`Full Spec.html:1043`) — it has no `opacity`/disabled treatment whatsoever; the dim-when-unclaimable behavior is this app's own added affordance, with no prototype source of truth to check a fix against.

**Recommending 3 real options rather than guessing at one:** leave as-is (now that the mechanism is understood), give the disabled CLAIM its own flat single-hue treatment (a real visual change), or make face/shadow the same hex specifically in the disabled state (stays purple, removes the seam, cheapest change). **This needs an actual Designer/Owner call, not a unilateral pick** — routed back rather than ticketed. **Update: Owner forwarded the question to Designer directly and asked to keep it postponed — nothing for anyone to build until that answer comes back.**

**Advisor next:** available, no open thread. **PM next:** nothing to do here until Designer answers.

---

**2026-09-15#7 — shipped in `#598`, deployed live at `rapidclash-00112-2z8` (confirmed independently via `gcloud run services describe`, matching PM's report exactly). Bot-crowd restarted, all bots online, `/open-challenges` verified live.** Designer's D14: the Rewards header showed the wallet pill + balance while logged out. Full detail: `ADVISOR_TO_PM.md` 2026-09-15#7.

- **Confirmed real, and the actual cause was neither of Designer's two guesses (no second header, no mock-user leak).** `RewardsHub.tsx:210` never threaded its already-available `loggedIn` prop into its `<HubRibbon>` call — `HubRibbon`'s own `loggedIn = true` default silently rendered the signed-in branch. The rest of the file used `loggedIn` correctly (VIP blur, Rakeback lock, `CardStatusRow`); this one call was the only gap.
- **Fix shipped:** `loggedIn={loggedIn}` added to that call, matching how `HomeHub`/`GameHub` already call the same shared component.
- **The recommended hardening shipped too:** `HubRibbon.tsx:74`'s own default flipped `loggedIn = true` → `loggedIn = false` (fail closed, not fail open). PM independently traced `ProfileHub`/`AffiliateHub`'s own identical gap and confirmed both are structurally auth-only (`ProfileHub`'s `token: string` is non-nullable by type; `AffiliateHub` always shows a real balance unconditionally) — passed `loggedIn` explicitly at both so the default flip doesn't turn their dormant gap into an actual regression. **All 5 `<HubRibbon>` call sites in the app now pass `loggedIn` explicitly** — independently verified via diff, matches PM's report exactly.
- **Closing ask (Games, Menu, Chat) confirmed clean** — no changes needed there.

Deployed at Owner's request. Nothing else pending on this ticket.

---

**D01-D13, the ENTIRE Designer collection, fully shipped AND deployed live at `rapidclash-00111-pvt`; the one remaining open question — item 4's "above" spacing — is CLOSED, Designer confirmed no change needed.**

**The full D01-D13 collection is closed out: every item processed, every real fix merged, and — as of this deploy — every merged fix is confirmed live in production, not sitting in a merged-but-undeployed queue.** `#587` (footer), `#591` (Open Games), and `#593` (Recent Games fade) shipped together in one deploy at Owner's request, revision `rapidclash-00111-pvt` (confirmed independently via `gcloud run services describe`, matching PM's report exactly). Bot-crowd restarted, all bots online, `/open-challenges` verified live, no errors.

**2026-09-15#6 — CLOSED. The last open thread from the whole collection: item 4's "above" spacing question (`2026-09-14#1`) is resolved with no code change.** Owner relayed Designer's answer: "Everything looks fine now." The live-measured 26px gap (vs. the originally-stated 35px target) is acceptable as-is — confirms the 35px figure was itself a screenshot-based overestimate (same class of miscount as items 1+2 in that same ticket, both of which also turned out to already be correct in production). **No further action needed from anyone.** This closes out every single item across the entire D01-D13 Designer package collection — nothing open anywhere.

**2026-09-15#5 — shipped in `#593`, merged and deployed live.** The last package in the collection (D13): Account page's Recent Games fade goes to grey instead of white in light mode. Full detail: `ADVISOR_TO_PM.md` 2026-09-15#5.

- **Confirmed real, and the mechanism is more precise than Designer's own guess:** it's not a hardcoded literal anywhere — `ProfileHub.tsx:599`'s fade is a `color-mix()` gradient built from a token, exactly like the prototype's own construction, just the WRONG token (`--rc-sunken` instead of `--rc-bg`).
- **Why this was invisible until light mode existed:** dark theme's `--rc-bg` and `--rc-sunken` are both `#0b0b0b` — identical values, so the wrong token has been a total no-op in dark mode. Light theme diverges (`--rc-bg: #ffffff` vs `--rc-sunken: #d3d3dd`) — `#d3d3dd` is exactly the grey in Designer's screenshot.
- **One small, functionally-immaterial correction to Designer's own citation:** the prototype's actual final gradient stop is `color-mix(in srgb, var(--rc-bg) 99%, transparent)`, not literally `var(--rc-bg) 100%` as quoted — a 1%-opacity difference, imperceptible, already matched by this file's own stops.
- **Closing ask (check Rewards + elsewhere) run:** only 3 files anywhere use a `linear-gradient(to bottom` fade — this one (the bug), the Footer's (already fixed correctly in `#587`), and Open Games' edge-fade (a different, theme-invariant `mask-image` mechanism, not a token bug). `RewardsHub.tsx` has no collapsed-list/VIEW MORE affordance at all — the ask doesn't apply there.

Fix was one line plus one new object key (`RC.bg`, added to `ProfileHub.tsx`'s token object).

**Advisor next:** available, no open thread — nothing pending on either end. **PM next:** nothing pending.

**2026-09-15#4 — shipped in `#591`, merged and deployed live.** Designer's spec for the Open Games section's light-mode theming — same root-cause class as the Footer ticket (hardcoded `#1A1A2E`/`#FFFFFF` literals instead of `var(--rc-surface)`/`var(--rc-text)`), all in one file (`GamesCarousel.tsx`). PM independently re-verified every citation before implementing — all matched. Full detail: `ADVISOR_TO_PM.md` 2026-09-15#4.

- **Items 1-4 (heading gone, LIVE pill dark, striped rows navy, row text white):** all confirmed exactly as diagnosed, all the same literal-instead-of-token fix. One small correction to Designer's own hex: the game name's literal is `#F2F2F6` (a near-white grey), not literally `#FFFFFF` — same failure mode either way.
- **Item 5 — a real correction, not just confirmation:** Designer's "tier icon missing on striped rows / handle missing on plain rows" was treated as one white-on-light bug; it's actually two different things. The handle half is real (folds into item 4's fix, nothing extra). The tier-icon half doesn't hold up — checked 11 rows directly on the live page: icon presence tracks `ownerTier === 'Unranked'` (renders nothing, by design, already documented from `2026-09-13#7`), completely independent of row striping. What Designer's screenshot caught was a coincidental sample, not a systemic bug — nothing to fix. PM independently confirmed the same finding.
- **Already confirmed correct, no change needed:** tab rail, JOIN buttons, stake amounts (green, theme-invariant), currency icon, and the thumbnail's `#1B1B2E` fallback (Designer's own named exception, confirmed also a deliberate literal in the prototype itself).

Deployed live alongside `#587`/`#593` in the same batch deploy, per Owner's timing call. Nothing else pending on this ticket.

**2026-09-15#3 — RESOLVED, same day. Designer sent "D12," byte-identical to D09/2026-09-14#1 (confirmed via file hash — same screenshots, same text, word-for-word). Investigating why Designer would still be seeing the already-fixed title-row bug surfaced a real deploy gap: `#582` and `#585` were both merged but never deployed — production was still on `rapidclash-00109-9hg`, the revision from the nav double-tap fix, with nothing shipped since.** PM deployed immediately on the finding (`rapidclash-00110-tb4`, confirmed live), restarted the bot-crowd, and independently verified the Pepe asset file is fully deleted with zero remaining references anywhere in the codebase — not just visually replaced. Owner was flagged the moment the gap was found and has since seen it resolved. Item 4's "above" spacing question (2026-09-14#1) was the only piece from this stretch still open at the time — since closed, see `2026-09-15#6` above.

**2026-09-15#2 — shipped in `#587`, merged to `main`.** Designer's spec for the footer's light-mode theming — all 6 numbered items confirmed real, no corrections needed to Designer's own diagnosis anywhere (the cleanest ticket of the batch so far). PM independently re-verified every citation before implementing (gradient rgba values, `--foreground`'s missing light override, `--rc-muted`'s dark-only `#83838F` value, `HubRibbon`'s existing logo-swap pattern) — all matched, nothing to correct. Full detail: `ADVISOR_TO_PM.md` 2026-09-15#2.

- **Items 1, 4, 5, 6 (gradient band, heading, link text, disclaimer/copyright):** all the same shape — a hardcoded literal (`#1A1A2E`/`#FFFFFF`/`text-foreground`/`#83838F`) where a theme token belongs. Item 5 traced precisely: `text-foreground` resolves to a legacy shadcn token (`--foreground`) with **no light-mode override anywhere in `index.css`** — the same bug class already fixed on `HubToolbar`/`HubRibbon` this week. Fixed locally (swapped to `var(--rc-text)`/`var(--rc-muted)` in this one file), not by re-theming the shared `--foreground` token globally, matching established precedent.
- **Item 3 (logo swap):** confirmed, and the dark-mode asset **already existed in the repo, already used correctly in the header** (`HubRibbon.tsx`) — pure copy-the-pattern reuse, zero new assets or logic.
- **Item 2 (footer background):** confirmed already correct, no change needed — Designer's own note said so, verified true.
- **"Also" (Tournaments→Events):** the label fix shipped with the rest; making it an actually-functional link needs new plumbing across 6 different call sites (no callback for it exists anywhere today) — split out and held as its own follow-up, not bundled in.
- **The closing ask (grep the tree for hardcoded hex) is real, and genuinely bigger than this ticket:** ran it — 41 real hits (after excluding legitimate icon-fill false positives) across **15 files**. Some are real bugs, some are deliberate theme-invariants already reasoned about elsewhere (e.g. Dice's own die-cube face colors) — this needs the same file-by-file human triage as the Footer, not a mechanical sweep. Held as its own dedicated follow-up ticket, not silently absorbed into today's fix.

**Both follow-ups (making Events functional; the repo-wide hex sweep) are flagged to Owner as open, non-urgent items — no timeline pressure from either Advisor or PM.**

Deployed live alongside `#591`/`#593` in the same batch deploy, per Owner's timing call. **Advisor next:** available, no open thread. **PM next:** nothing pending on this ticket or anywhere else in the collection — item 4's "above" spacing question is closed, see `2026-09-15#6`.

**2026-09-15#1 — shipped in `#585`, deployed and verified live at `rapidclash-00110-tb4`.** Designer's 3 Rewards fixes (avatar sync, RC→currency-icon, claim-button shift) all landed, plus the HIGH PRIORITY Pepe-image retirement — confirmed genuinely gone (asset deleted, no references left), not just hidden. Two small implementation notes worth keeping: `CurrencyIcon`'s USD face bakes its own `$` glyph as SVG text, so pairing it with a manually-`$`-prefixed span under one shared testid double-renders `$$` — scope separate testids to the amount span alone, matching how `GamesCarousel`/`ProfileHub` already do it. A stale "no `$` leaks into body" test in `RewardsHub.test.tsx` (predating `CHARTER.md` #4's currency-skin approval) was flipped per the already-documented "flip organically" policy. Full detail: `ADVISOR_TO_PM.md` 2026-09-15#1.

**2026-09-14#1 — CLOSED. Items 1-3 + item 4's "below" half shipped in `#582`, deployed and verified live; item 4's "above" half closed with no code change (see `2026-09-15#6`).** Designer's Games-page title-row spec — 2 of 4 claims confirmed already-correct via live measurement (font-size, icon-text gap), item 3's real cause found precisely by PM (a leftover legacy global `h2` CSS rule at `styles.css:57`, not Tailwind's fault), item 4's "below" gap fixed by moving margin from the row onto the grid/empty-states. Item 4's "above" gap sat open pending Designer confirm — PM independently re-ran the same citation check and reached the identical conclusion (the prototype's own source produces exactly 26px here, with no combination of nearby values summing to Designer's stated 35px target). Designer's answer, relayed by Owner: "Everything looks fine now" — the 26px live value is accepted as-is, confirming 35 was itself a screenshot overestimate (same class as items 1+2, both already correct). Full detail: `ADVISOR_TO_PM.md` 2026-09-14#1.

**2026-09-13#9 — shipped in #579/#580, deployed at `rapidclash-00109-9hg`, verified live.** Designer's report that the bottom nav needed a double-tap to activate was a real regression from #569 (2026-09-13#3's own press-feel fix) — `HubToolbar.tsx`'s bar-pop retrigger remounted the entire bar (all 5 buttons) on every `pointerdown` via a `key={pulseKey}` bump, destroying and recreating the exact button a tap landed on before the browser finished delivering that gesture's `click`. Confirmed via the test suite's own comment admitting the remount, with a test that structurally couldn't catch the failure mode it was named for ("remount-safe"). Fixed with a ref + imperative CSS reflow-trick (no state, no remount); PM's implementation also added a reflow-spy test proving the mechanism actually runs, stronger than what was originally sketched. Full detail: `ADVISOR_TO_PM.md` 2026-09-13#9.

**All 8 of the 2026-09-13 Designer packages (D01-D08) are merged to `main`, live in production, and demo-taker's bot-crowd restarted clean (all 32 bots confirmed back online).** PM sanity-checked the live `/open-challenges` feed post-deploy — `ownerTier` still present and correct on every entry. Ticket-by-ticket recap, newest first:

- **2026-09-13#8, shipped in #576.** Account's Recent Games list: empty-state styling fixed to match the Events-empty-state exactly, RC coin swapped for the wallet's currency icon + always-`$` amount (reusing the same global currency singleton #572 built), the opponent-row avatar circle dropped, VIEW MORE got its 3D ledge plus a corrected 520ms collapse transition and a proper opacity-fade (was popping via conditional mount — the 3rd occurrence this week of that pattern), the date format's locale-dependent bug fixed with explicit `en-US`+`hour12`, and game names now read "Rock Paper Scissors" instead of "Rps" via the same `nameByGame` pattern HomeHub/GameHub already use. One reported "bug" (loss-row amount color) was confirmed via direct code read to already be correct, shipped back on 2026-09-07 — flagged rather than silently re-touched; PM independently verified the same and dispatched without a live-page check, judging the source-level proof solid enough.
- **2026-09-13#7, shipped in #577.** Avatar picker rebuilt as an inline account-card strip (floating popup gone). The 10 new `rc-01`-`rc-10` presets (already in the repo) replace the 6 old named presets, retiring a real, previously-flagged licensing risk on 2 of them. Owner's call on the default-avatar conflict: the per-user disc color stays everywhere it's actually rendered (a real, already-shipped identity feature); only the picker's own reset-to-default tile shows the prototype's literal fixed-purple+glyph look, since that's a control affordance, not a rendered identity.
- **2026-09-13#6, shipped in #572.** Open Games tab rail + stake display + host tier icon. Tab rail got its shared track/ledge/press-sink/scroll-to-center, and the edge-fade technique corrected to a `mask-image` (it had been copying the category rail's overlay-div approach instead of its own). Stake display now always shows `$`, with the per-row/per-wallet currency icon reusing the existing `CurrencyIcon` component and a new `theme.ts`-shaped global singleton for the shared wallet-currency state (the same one #575/D08 above reused). Host tier icon: Owner's call (taken directly to them given the ADR-010 conflict) was to follow Designer exactly — tier icon on every host, bots included — while keeping the 🤖 marker fully intact in the underlying `ownerName` data (informed-consent requirement preserved) and stripping it only at render. New server-side `ownerTier` field added via the same `lookupUsername`-injection pattern chat's `resolveTier` already used.
- **2026-09-13#5, shipped in #570.** Rewards' logged-out gate removed; `RewardsHubScreen` made genuinely guest-safe (guarded fetch, explicit Rakeback lock condition instead of the earlier coincidental one), VIP-card blur added, claim-button 3D ledges + 2 new tokens added.
- **2026-09-13#4, shipped in #571.** Login/Signup rebuilt as the prototype's bottom sheet via a new shared `BottomSheet` component, built fresh against the verified spec rather than extracted from the affiliate `CreateCampaignSheet`'s old code (which had 4 real gaps of its own, fixed in the same PR by migrating it onto the new shared component too). Surfaced a real jsdom gap along the way (no native `PointerEvent` support) — fixed with a documented polyfill at the point of use (`apps/web/src/test/setup.ts`).
- **2026-09-13#3, shipped in #569.** Bottom nav's `rcNavBarPop` press feel added to the shared bar.
- **2026-09-13#2, shipped in #565.** Menu's Dark/Light control added; the first-open reveal-animation bug (sliding in from the wrong origin) fixed by reporting the Menu button's real position on mount, not only at click time.
- **2026-09-13#1, shipped in #566.** Games-hero category tiles (background/ledge/press/icon sizing), rail edge fades, and section title (icon + font) all fixed.

**Process notes from today's batch, worth carrying forward:**
- **The "conditionally-mounted overlay/element with nothing to animate from" architecture gap showed up 3 separate times** (Menu overlay #2, Auth sheet #4, Recent Games' VIEW MORE fade #8) — each fixed in its own shape rather than extracted into a shared abstraction (PM's call: one more instance isn't worth it yet). Watch for a 4th; that's the point to reconsider.
- **Two file-collision near-misses this week** (`HubToolbar.tsx` earlier, `ProfileHub.tsx` for D07/D08 today) — both from two D-tickets landing on the same file from a stale base, producing a silent duplicate-declaration merge (caught by `tsc`, not a conflict marker, since the two blocks landed on adjacent lines with no overlapping git context). PM's ask: flag a known file collision when a ticket is FIRST sent, not just discovered after the fact, whenever the Advisor already knows two in-flight D-tickets touch the same file.
- **A minor doc-comment inaccuracy** in D07's own `apps/web/src/lib/rail.ts` (claimed Affiliate tabs also used the shared rail helper — false, only 2 real consumers, not 3) was caught and corrected by PM before merging; confirmed this didn't originate from any Advisor ticket text.

*(D01-D08 batch's own close-out, before #9 was found above: fully shipped and verified, nothing pending at the time.)*

**Still queued from this batch, no scoping started:** the shared PLAY button's own missing `rcNavBarPop` (flagged 2026-09-13#3's survey, high leverage — one fix covers all 12 games — not yet ticketed); a possible "Related games" carousel's press feel (flagged same survey, not confirmed to exist in the app yet). Neither urgent. (See the older 2026-09-12 "Still queued" note below for the rest of the standing backlog.)

**2026-09-12#5 — shipped in #560.** Owner's live post-merge Dice pass (#558) found a real bug that also affected Mines' already-shipped #555 — same root cause, both games; `MinesPanel`/`DicePanel`'s live board was vanishing into the idle preview during the `'result'` phase. Fixed by adopting Coinflip's own `phase === 'in-match' || phase === 'result'` pattern. Also confirmed and fixed: Dice's missing matching-phase table dim, and cube "travel" via an honest position-slide (no fabricated numbers). Mines' live-play HUD text (own/opponent safe-counts, round number, status line, "Resign") was surveyed at Owner's request and categorized — most kept as real functional info with no prototype substitute, "Resign" flagged for an explicit Owner call, nothing dispatched from that survey yet. Full detail: `ADVISOR_TO_PM.md` 2026-09-12#5.

**2026-09-12#3/#4 — Dice vs. prototype, Owner's own analysis pass (first time playing it) — shipped in #558, superseded in part by #5 above.** Audio wired (play/dice-roll/dice-win.mp3 at their real trigger moments), reveal switched from popup to `ownBarResult` (the mechanism now shown by #5 to need the `phase==='result'` panel fix too), idle gauges un-dimmed, redundant idle text removed, panel resized to the prototype's fixed 266px box. History belt and cube colors were already correct pre-#558, no changes needed there. Full detail: `ADVISOR_TO_PM.md` 2026-09-12#3/#4.

**RPS round (2026-09-12#1) — closed out, shipped, confirmed good by Owner.** All 4 findings from Owner's `rapidclash-00102-km6` pass fixed in **#551**: `ownBarResult` removed from RPS (correction to #547 — RPS's real win indication is the card frame color + enlargement, `gameV` structurally excludes it, `Full Spec.html:3519`), z-index + 28% table-dim added during search, card enlargement at reveal (92→124px etc., `rpsExpanded()`), duplicate idle text removed. Owner's item-4 calls: Forfeit restyled smaller/subtler but **keeps its function** (no capability lost), duplicate "Waiting for an opponent…" dropped, "Picked {choice}…" kept as functional copy. Owner tested the deploy directly and confirmed: *"the latest deployment shows the RPS good looking and working as expected"* — forwarding to Designer for review. **RPS is done, no open thread.**

**Standing project stance (unchanged, still governs all of the below):** at this stage, visual/experiential fidelity to the Designer's prototype outweighs correctness. Where matching the prototype means accepting a gap the code was deliberately built to avoid, accept the gap and document why in a code comment rather than silently keeping the safer behavior.

**Merged and live (recap):** 2026-09-11#8's 3 tickets (#541/#542, plus C via #548), 2026-09-11#9's reversal (#543/#544), 2026-09-11#10's 2 items (#546/#547), 2026-09-12#1's RPS fix (#551).

**2026-09-12#2 — NEW: Mines vs. prototype, Owner's own analysis pass (not relayed secondhand) — 3 gaps, one is real feature work, not a tweak. Full detail: `ADVISOR_TO_PM.md` 2026-09-12#2.**

1. **Redundant idle text — clean removal, same pattern as RPS's fix in #551.** `MinesHub.tsx:182-184`'s paragraph ("Waiting for an opponent…" / "Choose a bet and press PLAY, or JOIN an open challenge") has zero prototype equivalent — the `isMines` idle block (`:470-529`) is just the tile grid + clock, no copy at all. Shared PLAY-button label already covers the waiting case.
2. **Board doesn't dim during search — real gap, cheap fix, mechanism already plumbed.** Prototype: `minesBoardOp: rpsMatching || mConverged ? 0.28 : 1` (`:3756`). `MinesPanel` (`MinesHub.tsx:393-399`) applies no opacity at all. `GameHub.tsx` already threads `barSlideActive` through `GameAreaArgs` (`:686`) — Mines just never consumes it. Fix: `opacity: args.barSlideActive ? 0.28 : 1`, same treatment RPS got in #551. Caveat: prototype's condition also covers the post-match `mConverged` phase (item 3), which doesn't exist yet — this fix only gets the search-phase half right until item 3(a) lands.
3. **The reveal is a real, bigger gap — not a broken feature, the default popup is firing because Mines never opted into the (correct, for Mines) shared mechanism.** `MinesHubScreen` wires neither `suppressResultOverlay` nor `ownBarResult`. Unlike RPS, `ownBarResult` **is** the right mechanism here — `gameV = view === 'mines' || isDice` (`:3519`) is the exact gate that turns on the bar win-fill. Three distinct parts:
   - **(a) Bars converge to center post-match — genuinely new `GameHub` plumbing**, not existing today. `mConverged = mActive && !isDice` (`:3524`) drives a *different* shift magnitude (`mRShift` fallback `{o:100,p:-100}`, `:3529`) than the search-phase slide (`mShift`, `{o:123,p:-123}`) — real, distinct values. `GameHub`'s `barSlideActive` (`:615`) only covers `matchForming||searching` today, nothing post-match.
   - **(b) Win/lose/draw frame ring — just wiring the existing, Coinflip-proven mechanism.** `playerBarRing`/`winFillAnim`/`winTextAnim` (`:3787-3789`) map exactly to `GameHub`'s `ownBarResult`/`ownBarVerdict` (`:691-704`). Fix: add `suppressResultOverlay` + `ownBarResult` to `MinesHubScreen`'s `<GameHub>` call, Coinflip's own reference pattern. Correction to Owner's description: the ring is **own-bar only** (`oppBarRing: 'none'` unconditionally, `:3786`) — no symmetric opponent-side ring to build.
   - **(c) Gem-count text above/below the pills — new content, no generic slot exists.** `oppGemText`/`myGemText` (`:467`/`:690`) sit outside the bar box entirely (above opponent's, below own), gated by `mReveal`/`mGrown`. Correction to Owner's description: this text is **always green**, win or lose — the win/lose color-coding lives entirely on the frame ring (b), not this text. `renderSlotAside` renders inside the bar row, not outside it — needs a new prop or slot extension.
   - **Scoping call:** (b) is a same-night wire-up. (a) and (c) are real new features, closer in size to #546's original bar-slide build. Recommend shipping items 1+2+3(b) together (cheap, no new plumbing), scoping (a)+(c) as their own follow-up ticket.

**Advisor next:** available, no open thread — 2026-09-12#2 is dispatched (PM's ticket, items 1+2+3(b), being built/reviewed; 3(a)+3(c) still to be scoped as a follow-up). **PM next:** merge once clean; hold the deploy until Owner is back to confirm.

**Infra note, 2026-09-12 — not a migration item, filed here since it's the nearest live tracker.** Deploying tonight surfaced a real production issue, not a design gap: the Cloud Run service was on the 512Mi default and got OOM-killed repeatedly after the chat/RPS/chess-stakes feature batch grew real per-connection in-memory state, dropping every WebSocket in a loop — looked identical to the already-documented "bot-crowd looks dead" (`DEPLOY.md` §3b) from the outside, but was a different root cause (the whole service flapping, not one stale revision). Fixed live via `rapidclash-00104-djs` (bumped to `--memory 1Gi`); the canonical deploy command and a new `DEPLOY.md` §3c now bake that in and describe how to tell the two apart. **Re-check memory headroom after any future feature batch that adds real per-connection state** — this won't be the last time a feature batch grows what a single connection holds in memory.

**Still queued, no scoping started:** RpsIdle/MinesIdle's own `opacity-50` idle-dim echo (same "invented dimmed-anchor convention" root cause as Dice's item 5, both already shipped) — small standalone follow-up whenever convenient; races/leaderboards (real feature, tabs already show a graceful placeholder toast today); lobby/stake-entry/play/result 4-screen cleanup (confirmed dead code, zero user-visible impact, pure tech debt). None urgent.

**Process:** Advisor → worktree `worktree-advisor-migration`; PM → primary checkout; each coder → its own agent worktree.

**Standing lesson from this session, worth remembering:** several confident-but-wrong-or-nearly-wrong moments tonight (a tile-art misread, the "no design source exists" claim, reading `MinesHub.tsx` on a stale branch, and briefly assuming a GCP-access problem that was actually just a wrong project-name guess) all trace to the same root cause — trusting a quick check instead of looking at the current, real thing. Every one was caught by going one step further and actually looking (a screenshot, a `git log`, the real project list, the real server logs) — the #532 investigation itself is the clean version of that same instinct done right: don't stop at "bots are back," check what's actually happening underneath.

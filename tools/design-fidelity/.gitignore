# Generated app captures and diff images — regenerated on every run, never committed.
captures/
diffs/

# The prototype reference set. Committed once the capture format is finalised (frame-cropped,
# stable per-screen nav) — until then it is regenerated locally via `pnpm capture:prototype`.
references/
